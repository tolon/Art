# Feature Matrix

What is actually built, where it lives, and what proves it.

Spec §10 and §89 are binding here: **never claim support until it is
implemented and tested.** A row may only be marked ✅ if there is working code
*and* a test. If you are tempted to write "mostly works", it is 🟡.

| | Meaning |
|---|---|
| ✅ | Implemented and covered by tests |
| 🟡 | Partly implemented — the gap is named in the row |
| 🔩 | Stub only: the module exists and returns `NotImplemented` |
| ⏳ | Not started |
| — | Not applicable |

Scheduling lives in [STATUS.md](STATUS.md); defects live in [ISSUES.md](ISSUES.md).

---

## Which machines

ART is not an A500 tool that tolerates other Amigas. Most of what it does is
machine-independent — a format does not know which machine wrote it — and
where the machine matters it is carried as a **machine profile**, not a code
path. Built-in presets: **A1000, A500, A500+, A600, A2000, A3000, A1200,
A4000, CDTV, CD32** (`core/profile.rs`, read by the WinUAE launcher and the
compatibility check).

Commodore's 8-bit files are in scope as well, as of 2026-08-12, and read-only:
D64, D71, D81 and T64 open in the same commander an ADF does; TAP, PRG and CRT
are identified and described rather than browsed. See the C64 rows below.

## Format support

Mirrors the spec §10 table. "Detect" means ART can identify the format;
the other columns mean it can act on it.

| Format | Detect | Read | Create | Edit | Validate | Convert |
|--------|:------:|:----:|:------:|:----:|:--------:|:-------:|
| **ADF** (DD, OFS/FFS) | ✅ | ✅ | ✅ | ✅ | ✅ | ⏳ |
| **ADF** (HD) | ✅ | ✅ | ⏳ | ✅ | ✅ | ⏳ |
| **ADZ** | 🟡 | ⏳ | ⏳ | — | ⏳ | ⏳ |
| **DMS** | 🟡 | ⏳ | — | — | ⏳ | ⏳ |
| **HDF** | ✅ | ✅ | ✅ | ✅ | 🟡 | ⏳ |
| **HDZ** | 🟡 | ⏳ | ⏳ | — | ⏳ | ⏳ |
| **LHA** | ✅ | ✅ | ⏳ | ⏳ | 🟡 | — |
| **ZIP** | ✅ | ✅ | — | — | 🟡 | — |
| **7z** | ✅ | ✅ | — | — | 🟡 | — |
| **ISO9660 / Joliet** | ✅ | ✅ | — | — | 🟡 | — |
| **ROM** | ✅ | ✅ | — | — | ✅ | — |
| **D64 / D71 / D81** (C64 disk) | ✅ | ✅ | — | — | 🟡 | — |
| **T64** (C64 tape archive) | ✅ | ✅ | — | — | 🟡 | — |
| **TAP / PRG / CRT** (C64) | ✅ | — | — | — | — | — |

Notes:

- 🟡 **Detect** = extension-only, no signature check. Confidence is reported
  honestly (`core/detect.rs`), so nothing is claimed that was not verified.
- **ADF (HD)** — reads, writes and validates. The DD-only assumption that made
  these fail was ART-037/038; geometry now comes from the image. Edit is
  proven at 3520 blocks through the write gate
  (`an_hd_floppy_is_written_through_the_gate`); create still only produces DD
  images.
- 🟡 **LHA validate** — structure and path safety are enforced; per-entry CRC
  checking is not implemented.
- **ZIP / 7z / ISO9660 — read-only, and permanently so.** ART reads archives
  and discs; it writes neither, in any direction, and the panes refuse it by
  saying so rather than by doing nothing. All three go through the one
  extraction gate (`core/archive/extract.rs` for archives), so the traversal
  check, the output caps and the "declared size is a claim" check are the same
  code for each — proved by one hostile-archive test every backend is run
  through.
  - 🟡 **validate** = the gate refuses malformed and hostile input, and
    reports what it refused; there is no per-entry checksum verification.
  - ZIP is deflate only; encrypted entries are refused by name. 7z is LZMA.
  - ISO9660 covers Joliet and both raw sector layouts (Mode 1 and Mode 2/XA
    Form 1); Mode 2 Form 2 is refused rather than misread. **Rock Ridge `NM`
    names and the Amiga `AS` entry are read** (ART-078, 2026-08-20), so a
    Unix-mastered Amiga CD with no Joliet descriptor keeps its mixed-case
    names, and a file's AmigaDOS protection bits and comment survive a copy —
    onto a volume through the copy engine, into a folder through a `.uaem`
    sidecar. `SL`, `RE`, `CL`, `PL` are not read; no measured disc carries one.
  - Verified against **7-Zip's independent implementation**
    (`scripts/iso-oracle-check.py`), raw layouts and a Rock Ridge disc
    included, the raw ones via sector-stripping. The `AS` entry is beyond what
    7-Zip reads, so it rests instead on the specification read off the owner's
    own Developer CD v2.1, on ODFileSystem's unit vectors reproduced byte for
    byte, and on 44 796 real entries decoding exactly
    (`scripts/iso-susp-census.py`). Not against a real Amiga CD filesystem
    running on an Amiga; nothing claims otherwise.
- **C64 formats — read-only, like every other container ART opens.** D64,
  D71 and D81 (35- and 40-track, with or without error bytes), and T64 tape
  archives, open as panes and copy out to a folder. Writing one is not
  implemented and not planned.
  - 🟡 **validate** = malformed images are refused rather than misread — an
    unknown size with the size in the message, a track or sector outside the
    disk, a sector chain that loops. There is no BAM-versus-directory
    consistency check.
  - **A T64's header is not trusted**: `used = 0` with real records lists the
    records, and an end address the file cannot support is clamped to what is
    actually there. The pane says when it had to do either.
  - `.tap`, `.prg` and `.crt` are **identify-only by design, not by
    schedule** — `c64.identify` reports what the file is, how big it is and
    why there is nothing inside to open. A TAP is the tape signal sampled as
    pulse widths: no directory, no file table.
  - Names are PETSCII with `0xA0` padding stripped from the end only. The
    graphics set renders as `·` rather than guessed-at Unicode look-alikes.
- **ADZ / HDZ / DMS** decompression is Stage 5 work; `xdms-rs` is the candidate
  for DMS (see `ART-kaynak-listesi.md`).

## Filesystems

| Filesystem | Read | Write | Notes |
|---|:---:|:---:|---|
| **OFS** | ✅ | ✅ | Data blocks carry the 24-byte header |
| **FFS** | ✅ | ✅ | Raw data blocks |
| **FFS INTL** | ✅ | ✅ | Case folding honoured in name hashing ([ART-010](ISSUES.md#fixed)) |
| **Dircache** | 🟡 | ⏳ | Read-only on purpose: a directory is stored twice, and writing one copy corrupts listings on a real Amiga (§3.4) |
| **FFS/OFS on HDF partitions** | ✅ | ✅ | Stage W: multi-block bitmaps, journalled in-place writes |
| **PFS3** | 🟡 | 🟡 | **Not in this table's subject — the volume browser.** `core/volume` does not mount a PFS3 volume and says so (`DosType::label` → "PFS3 — not supported yet"), so `/files` cannot open one. ART does read and write PFS3 elsewhere, through `libpfs3`: `core/preload/native.rs` formats a card's partition and copies a tree in, and `core/osinstall/verify.rs` reads it back to check a manifest. One gap in that writer is named rather than hidden — non-ASCII AmigaDOS names ([ART-113](ISSUES.md)) |
| **SFS** | ⏳ | ⏳ | Listed with its name and "not supported yet" |

---

## Modules

### Core engine

| Feature | Spec | State | Code | Tests |
|---|---|:---:|---|---|
| Format detection | §4 | ✅ | `core/detect.rs` | ✅ |
| SHA256 hashing (streaming) | §58 | ✅ | `core/hashing.rs` | ✅ |
| Atomic writes | §57 | ✅ | `core/safety/atomic.rs` | ✅ |
| Generational backups | §57, §93 | ✅ | `core/safety/backup.rs` | ✅ |
| Path-traversal defence | §56 | ✅ | `core/security/path.rs` | ✅ |
| Workflow Engine + catalogue | §5, §46, §91 | ✅ | `core/workflow/` | ✅ |
| Job Queue (progress + cancel) | §54, §55 | ✅ | `core/jobs/`, `commands/jobs.rs` | ✅ |
| Operation Log | §53 | ✅ | `core/oplog/` | ✅ |
| Error IDs (`ART-*`) | §68 | ✅ | `core/error.rs` | ✅ |
| Compatibility Engine | §34 | 🔩 | `core/compatibility.rs` | — |
| Generic validation surface | §69 | 🔩 | `core/validation.rs` | — |

### ADF Studio

| Feature | Spec | State | Code |
|---|---|:---:|---|
| Open, browse, volume info | §11 | ✅ | `core/adf/{mod,fs,blocks}.rs` |
| Extract file | §11 | ✅ | `core/adf/extract.rs` |
| Add / delete / rename / mkdir | §11 | ✅ | `commands/adf.rs` → `core/volume/write/` |
| Create blank / formatted / bootable | §11 | ✅ | `core/adf/create.rs`, `core/adf/bootcode.rs`. Until [ART-063](ISSUES.md#fixed) was fixed and verified by booting `test/art-bootable-test.adf`, `bootable` wrote a valid-looking but non-functional boot block (a bare `RTS`) — the boot block now reaches a CLI prompt on a real Kickstart/Workbench (not Workbench itself: `S/Startup-Sequence` and AmigaOS's own commands are content ART cannot supply). **Verified on real hardware 2026-08-12**: a real A500/A500+ with Kickstart 3.9, booting the image off a Gotek |
| Validate (boot block, checksums, bitmap) | §11, §12 | ✅ | `core/adf/validate.rs` |
| Optimisation analysis | §13 | ⏳ | — |
| Drag files in / out of the image | §11, §90 | ✅ | `/files` two-pane manager |

### Commodore 8-bit reader

| Feature | Spec | State | Code |
|---|---|:---:|---|
| Sector geometry (D64 35/40 track, D71, D81) | §10.5 | ✅ | `core/cbm/geometry.rs` — every zone boundary pinned |
| PETSCII names | §10.5 | ✅ | `core/cbm/petscii.rs` — `0xA0` stripped from the end only |
| Directory and file sector chains | §10.5 | ✅ | `core/cbm/d64.rs` — step limit *and* visited set, both proved by self-referencing fixtures |
| T64 tape archives | §10.5 | ✅ | `core/cbm/t64.rs` — records over header, ranges clamped |
| Identify-only formats (TAP, PRG, CRT) | §10.5 | ✅ | `core/cbm/mod.rs::identify`, offered as `c64.identify` |
| Open as a pane, copy files out | §10.5 | ✅ | `commands/cbm.rs`, `/files` |
| Writing any Commodore image | — | — | Not implemented, not planned |

### LHA Studio

| Feature | Spec | State | Code |
|---|---|:---:|---|
| Open, list entries | §14 | ✅ | `core/lha/mod.rs` |
| Safe extraction (traversal + bomb guard) | §14, §56 | ✅ | `core/lha/safe_extract.rs` |
| Overwrite policy | §89 | ✅ | `core/lha/safe_extract.rs` |
| WHDLoad detection with confidence | §15 | ✅ | `core/lha/whdload.rs` |
| WHDLoad pack layout (drawer, slave, icon) | §82 | ✅ | `core/whdload/mod.rs` |
| Per-entry CRC test | §14 | ⏳ | — |
| Create archive (lh5 compression) | §14 | ⏳ | — |
| One-click WHDLoad install to HDF | §14, §82 | ✅ | `core/whdload/`, `commands/whdload.rs`, `/whdload`; cross-checked against amitools |
| Install an archive into an ADF | §14, §41.5.6 | ✅ | `core/sources/install.rs` |

### Hard Disk / RDB

| Feature | Spec | State | Code |
|---|---|:---:|---|
| Open HDF, report geometry | §16 | ✅ | `core/hdf.rs` |
| Create HDF with RDB (sparse) | §16 | ✅ | `core/hdf.rs`, `core/rdb.rs` |
| RDB parse, checksum, partitions | §18, §19 | ✅ | `core/rdb.rs` |
| Read a PiStorm card (MBR + several RDBs) | SD-2a | ✅ | `core/mbr.rs`, `core/card/mod.rs` — verified against CaffeineOS 9317 and MultibootOS 2.2. A plain HDF reads as one area at offset zero, so callers do not branch |
| Plan a card's shape and write its partition table | SD-1 G2 | ✅ | `core/mbr.rs::plan_card` + `write_mbr` — FAT32 first at LBA 2048, one to three `0x76` areas, 4 MiB aligned. Defaults measured off both real cards; asked for MultibootOS's shape it produces MultibootOS's layout to the sector. **An Amiga disk at byte zero is not expressible**, which is how SD-0's unit-0 rule is enforced |
| Format the FAT32 boot partition | SD-1 G2 | ✅ | `core/fat32.rs` — FAT32 forced (a small partition would silently be FAT16 and not boot), 4 KiB clusters as on both real cards, files placed in the root. Every write is bounded to the partition by `Region`, so a formatter cannot reach the Amiga's first RDB. Cross-checked against **7-Zip** (`scripts/fat-oracle-check.py`): filesystem type, geometry, label, names and every file's bytes |
| Place the Emu68 payload on FAT32 | SD-1 G2 | ✅ | `core/card/payload.rs::emu68_payload` — unpacks the user's archive through the one security gate, refuses one that is not for their board or release line (and names `Emu68-raspi.zip` for what it is), merges the Pi's own `config.txt` instead of regenerating it, writes `cmdline.txt`, and places the Kickstart under the name the config points at. Driven end to end with the real release |
| Build a card image end to end | SD-1 G2 | ✅ | `core/card/build.rs::build_card` — sparse image, partition table, formatted boot partition, and an RDB at the start of every Amiga area at its own offset. Reads itself back with the reader that opens real cards; refuses to build over an existing file. The volumes inside are **not** formatted: that is SD-2's PFS3 work, and nothing claims otherwise |
| Building a card reaches the UI | SD-1 G2 | ✅ | `commands/card.rs::card_plan_build` + `card_build` on the Job Queue, and the OS Builder's **boot-only card**. One request type and one spec mapping for both, so a screen cannot show one card and write another; the plan answers `SAFE_CREATE` before the button rather than by a job that fails, and the result is the card read back by ART's own reader. Warnings are typed, not sentences, so they reach the user in their own language |
| Build manifest, and a card checked against it | SD-1 G7 | ✅ | `core/card/manifest.rs` — written beside the image as `<card>.manifest.json`, and **read off the finished card** rather than remembered from the build, so it records what is there. Carries the archive's and the ROM's SHA-256, the partition table's, every boot file's, and a 256 KiB window at each RDB. `card_verify_manifest` checks the table, the areas and the RDBs; the boot partition's files it reports as **not checked**, because ART writes FAT32 and cannot read one — `scripts/fat-oracle-check.py <card.img>` answers those with 7-Zip |
| Image health check — the last gate before the file is handed over | SD-1 G8 | ✅ | `core/card/health.rs` + `card_check_image`. Fourteen checks on a card ART built: the boot partition is first (SD-0's unit-0 rule) and at sector 2048, the areas are 4 MiB aligned, nothing overlaps or runs past the end, every RDB reads and checksums, no partition names a filesystem the card lacks (ART-084 as a gate), the manifest still agrees, and the four files the firmware needs are there. **Three states, kept apart**: pass, fail, and `not-checked` — the last never rendered as a tick. Plus the steps only the user can take (§50): flash it, HDMI before power, check the Pi, the volumes will ask to be formatted |
| A build as a drop target | SD-1 G15 | ✅ | `core/card/intake.rs` + `card_intake`. The one drop pipeline asked the *other* question: not "what can I do with this file" but "what does it become in this card". An Emu68 archive and a Kickstart fill their fields; a `config_<name>.txt` is recognised and declared not-yet-used (G16); **everything Amiga is told it needs a volume this card has not got** — the answer SD-1 owes most often, said rather than swallowed. The archive is matched **by name**, deliberately: its bytes say "zip", and which board and line it is for lives only in the name (ART-091), so the answer carries every reading rather than picking one |
| Format Amiga volumes and fill them | SD-2 G3/G5 route E and native | ✅ | `core/preload/` — the plan, the `VolumeFormatter` trait, and **two** implementations: `native::NativeFormatter` (`libpfs3` for PFS3, ART's own writer for FFS, launches nothing) and `tools/hst_imager.rs`. `commands/preload.rs::run_with_fallback` tries native first for every step and reaches `hst-imager` only for the two known capability gaps ([ART-113](ISSUES.md)'s non-ASCII PFS3 names, [ART-117](ISSUES.md)'s foreign-RDB embed) — reachable from the product as of [ART-120](ISSUES.md), fixed 2026-08-16, having shipped built-but-unreachable since G5. Run against the real tool on a card ART built (RDB embedded, `DH0` formatted as `Work`, a tree copied in, listed back by the tool as 1 directory, 2 files, 20 B) and checked against it as an independent oracle both directions (`scripts/pfs3-oracle-check.py`). Refuses before anything runs when a partition names a filesystem the card lacks (ART-084 again). **Neither writer's output is read back and checked within this operation** — see the OS-install rows below for where ART's own PFS3 reader (the same `libpfs3`) is used, on a different screen |
| Preparing volumes reaches the UI | SD-2 G3/G5 route E and native | ✅ | `src/lib/preload.ts` + the OS Builder's **prepare a card's Amiga volumes**, its third kind. §92 in full: read the card → choose what gets erased → preview → confirm the count → run as a job → report. **Nothing is chosen to start with and the choices are not remembered**, unlike every path on the screen — a screen that came back armed to erase two partitions would be ART's own remembering rule turned into a hazard. The volume-name rules are `check_name`'s, checked before the format rather than during it. `hst.imager`'s path is a setting beside `winuaePath`, no longer required for an ordinary run ([ART-120](ISSUES.md)) — only when the plan already shows the one step that always needs it, or a step's own content turns out to. The preview names which writer is expected to run each step, before the confirmation; the result panel names which one actually did, per step, never silently, and says plainly that no writer's output is read back and checked here (§89) |
| Does the card's Kickstart suit the OS about to go on it | SD-2 G9 | ✅ | `core/rom/pairing.rs::compare` + `preload_rom_pairing` + the volume-preparation screen. Reads two already-recorded facts rather than any ROM bytes — the tree's own planning ROM (`core::osinstall::PairedRom`, from G5's `distribution.json`) and the card's manifest (G7's `SourceFacts::kickstart_stated_major`, recorded at build time because ART writes FAT32 and has no reader for it) — and compares only what the tree's own recipe required, never "is this the same ROM": a different Kickstart is ordinary, an older one than the recipe named is not. Four verdicts (`Paired`, `Suitable`, `Unsuitable`, `NotChecked`), the last **never** rendered as a pass (§89) — a folder that is not a distribution tree, or a card ART did not build, is left alone rather than guessed at. Shown before the confirmation checkbox, beside the writer-choice line G3/G5 already print. **Run against the pairing that actually failed under WinUAE with a licensed ROM on 2026-08-16 (real hardware untouched)**: the real AmigaOS 3.2 tree requiring Kickstart 47 against a card built with the real Kickstart 40 comes back `Unsuitable { needs: 47, found: Some(40), rom: "kick.rom" }`; the same recipe's V40 build, which carries its own compatibility modules, comes back `Paired` against the identical card, since it is the same ROM file the card and the tree's own plan both hashed. Against a **second** card built from the user's real Kickstart 47 — a ROM it was never built for — that same V40 build comes back `Suitable { rom: "kick.rom" }`, which is the only evidence on real material that the check reads the tree's own capability rather than comparing version numbers, since 40 is not ≥ 47 (`commands::preload::tests::the_real_trees_against_a_real_card_when_asked`, `#[ignore]`d, env-gated — both cards it builds to prove this are deleted at the end of the run). The verdict is asked and rendered **per content folder**, named by its drive, and the requirement is evaluated before identity is ever consulted ([ART-129](ISSUES.md)) |
| What goes where — turn a pile of files into a staging tree | SD-2 G11 | ✅ | `core/layout/` (`scan.rs`, `policy.rs`, `mod.rs`, `apply.rs`) + `commands/layout.rs` + its own **layout** screen (`/layout`, `src/pages/ContentLayout.tsx`) — a peer of the OS Builder in the sidebar, not a screen inside it; the staging folder it produces is pointed at **by hand** from the preload screen. Walks a drop, stopping at a WHDLoad drawer rather than descending into it, and proposes a destination per item: `Games`, `Floppies`, `HardDisks`, `CDs`, `Unsorted`. A ROM and a Commodore 8-bit disk are **refused with a reason** rather than placed — a ROM is the FAT32 boot partition's business, a C64 disk has none on an Amiga volume. Unpacking a WHDLoad archive goes through the one archive gate and places the drawer's `.info` **beside** the drawer, never inside it (§82). Never overwrites an existing destination, never touches the source (tested byte for byte), `safe_join` on every destination since it is user-typed text, cancellable between items with `CancelledPartway` reporting how many landed. **ART cannot tell a demo from a game** — nothing in a `Detection` says so — so the policy proposes only what it can justify and the preview table is editable, not a cleverer rule engine. Driven in headless Chrome against the real bundle, with `invoke` mocked — **not yet driven in `pnpm tauri dev` against real material, and no staging tree it built has been carried onto a card** |
| Filesystem drivers as the card's, not the area's | SD-2a | ✅ | `CardImage::partitions_missing_driver` — MultibootOS's second RDB carries no PFS3 while all fifteen of its partitions are PFS3, and the card works |
| Card reading reaches the UI | SD-2a | ✅ | `commands/card.rs::card_open` + the Hard Disk studio's card view. It asks the card reader for **every** file and branches on whether a partition table was found, so an HDF is unchanged and a card no longer has to be recognised by its extension. Read-only, and the screen says so |

| Read embedded filesystem drivers (FSHD + LSEG) | §18, SD G4 | ✅ | `core/rdb.rs::parse_file_systems`; the studio names partitions nothing will mount (`@/lib/rdbDrivers`) |
| Embed a filesystem driver into a new RDB | §16, SD G4 | ✅ | `core/rdb.rs::create_rdb_layout`; wizard step 4 (`@/lib/fsDriver`). Version read from the driver's `$VER:`. **Verified by a Kickstart, not only by tools** — which is the whole story of [ART-126](ISSUES.md): `rdbtool` extracted the driver back byte-for-byte and `hst-imager` listed it, and AmigaOS still ignored it, because `PatchFlags` named `StackSize` (`0x10`) where it had to name `SegListBlock`+`GlobalVec` (`0x180`, the value both of the user's real booting cards write). Until 2026-08-16 no disk ART made had ever been mounted by an Amiga; one now boots to a Workbench under WinUAE with a licensed ROM |
| Partition layout validation | §18, §89 | ✅ | `core/rdb.rs` |
| Browse partition contents | §17 | ✅ | `core/volume/`, `commands/volume.rs` |
| Write into a partition | §17, §57 | ✅ | `core/volume/write/` — OFS/FFS/INTL. Dircache and long-filename volumes stay read-only, with the reason |
| Undo journal for large images | Stage W §2 | ✅ | `core/volume/journal.rs`; recovery offered at mount, proved by a real process-kill test |
| Edit partition contents | §17 | ✅ | `/files`: copy, rename, move, delete, mkdir, attributes |
| Resize, clone, repack, migrate | §21–§24 | ⏳ | — |
| Snapshots | §49 | ⏳ | — |

Audited in Stage 3; see [ART-021 … ART-027](ISSUES.md#fixed) for what was
wrong. Stage R made partition contents reachable: `BlockDevice` +
`VolumeGeometry` let the existing FFS/OFS code read any geometry, so an HDF
partition browses like a floppy. Cross-validated against `amitools` — which is
how [ART-032 … ART-035](ISSUES.md#amiga-format-compatibility-stage-r-oracle)
were found.

Stage W added writing. Two strategies behind one API: an image of 16 MiB or
less is read whole, mutated in memory, validated and written back atomically —
the audited ADF pipeline, unchanged, so every floppy takes exactly the path it
always did. Anything larger is written **in place under an undo journal**,
because backing up two gigabytes before every rename is not a cost anyone
should pay per keystroke.

The write matrix, and why:

| DosType | Write | Why not |
|---|:---:|---|
| `DOS\x00`, `DOS\x01` — OFS/FFS | ✅ | |
| `DOS\x02`, `DOS\x03` — INTL | ✅ | Hashing follows the volume's own flag ([ART-010](ISSUES.md#fixed)) |
| `DOS\x04`, `DOS\x05` — dircache | ⏳ | A directory is stored twice; writing one copy leaves listings that look right in ART and wrong on a real Amiga |
| `DOS\x06`, `DOS\x07` — long filenames | ⏳ | A different directory layout, not a flag; reading is refused too |
| PFS3, SFS | ⏳ | Not mounted at all — listed by name with the reason |
| Bitmap marked invalid | ⏳ | Allocating from a map that may not describe the disk is how two files come to own the same block |

Everything above is cross-checked against `amitools` in both directions —
ART writes and amitools reads, amitools writes and ART reads — on FFS, OFS, a
64 MiB volume with 33 bitmap blocks, and protection bits. Crash recovery is
proved by a test that really kills the process at three points mid-write and
checks the image comes back byte for byte.

### Emulation & hardware

| Feature | Spec | State | Code |
|---|---|:---:|---|
| WinUAE detection | §35 | ✅ | `core/winuae.rs` |
| `.uae` config generation | §35 | ✅ | `core/winuae.rs` |
| Launch session | §35 | ✅ | `core/winuae.rs` |
| Machine profiles (presets) | §33 | 🟡 | `core/profile.rs` — the classic line, end to end: A1000, A500, A500+, A600, A2000, A3000, A1200, A4000, CDTV, CD32. Presets only; user-defined profiles are not built yet. A preset pins a Kickstart hash only where there is one to pin — an A1000 loads its Kickstart from floppy, and an A2000 or A3000 may run 1.3, 2.04 or 3.1, so those pin none rather than a guess |
| Kickstart ROM identification | §32 | ✅ | `core/rom.rs` |
| Gotek scan + FlashFloppy config | §37, §39 | ✅ | `core/gotek.rs` |
| Gotek bulk workflow | §38 | ⏳ | — |
| PiStorm hardware matrix (Amiga × board × Pi) | §40 | ✅ | `core/pistorm/hardware.rs` — kernel archive, storage device name, token gating and per-combination notes all derive from it |
| PiStorm `cmdline.txt` options | §40 | ✅ | `core/pistorm/options.rs` — one field per documented Emu68 token, merged never regenerated. Four profiles, each the tokens it writes (ART-090) |
| PiStorm `config.txt` firmware | §40 | ✅ | `core/pistorm/firmware.rs` — kernel, `initramfs`, display presets, opt-in overclock. Merged; the user's own lines survive |
| PiStorm Kickstart through ROM Manager | §32, §40 | ✅ | every ROM on the card identified by checksum; pick one from anywhere and copy it on, named and confirmed. Unrecognised is a label, never a refusal |
| PiStorm kernel version detection | §40 | ✅ | `firmware.rs::version_from_kernel` reads the `$VER:` string Emu68's own build compiles in; "unknown" when it says nothing |
| PiStorm kernel update from GitHub | §40 | ⏳ | F4's second half — not built; the screen offers nothing rather than a button that does nothing |
| PiStorm card verified on real hardware | §40 | ⏳ | no card built by this screen has been booted |
| PiStorm WiFi / network pre-seeding | §40, SD G14 | ⏳ | Amiga-side (`wifipi.device`, `DEVS:NetInterfaces`), so it belongs to volume building — declared on screen rather than offered |
| Multiboot `config_<name>.txt` sets | SD G16 | ✅ | list, create, duplicate, rename, activate, delete — each through preview → backup → write. A deleted set is kept; the one the card is running cannot be deleted |
| Distro profile registry | SD G13 | ✅ | `core/distro/registry.json` — data, not code; CaffeineOS, CoffinOS, AmiKit and two ART Baseline entries. No profile can name a token Emu68 lacks |
| OS Builder: licence, ROM family and card checks | SD G13 | ✅ | `/os-builder` — what each distribution requires of you, and whether the ROM and card you have suit it |
| OS Builder: prepare the card | SD-2a | ⏳ | blocked on inspecting a real distribution's layout by hand (research §8.2). Every profile is `available: false` and the screen says so |
| OS install: build a distribution tree from the user's own AmigaOS 3.2 media | SD-2 G5 | ✅ | `core/osinstall/{scan,plan,apply}.rs` — install ADFs found by the volume name recorded *inside* them, never by filename; which components switch on (required, chosen, or a satisfied `Condition` such as pre-V47 `modules-a1200`) resolved once and shared by the preview and the build; file-level destination collisions across two disks refused by name; the tree written with a `.uaem` sidecar per file and a `distribution.json` manifest (path, component, medium, SHA-256, protection, per file). 124 `core::osinstall` unit tests plus 21 `commands::osinstall` unit tests, 145 in all (synthetic fixtures — ART ships no Amiga content, so none of them can guard a *recipe-data* mistake that only real media exposes — except two: `core::osinstall::apply`'s `inspect_real_media_when_asked` and `run_the_real_engine_against_the_users_own_media_when_asked` are `#[ignore]`d hooks that run against the user's own copyrighted media, not a fixture). Run for real against the user's own 36-ADF 3.2 set and the paired Kickstart, and its own assertions now pin the numbers: 26 components on (`modules-a1200` among them, switched on by its own condition against the real V40 ROM, never chosen), exactly 4030 files / 330 directories / 11.9 MB written as plan items — the tree that produced holds 3933 files (the manifest included) and 280 directories, because an override writes one destination twice and a shared directory is counted twice ([ART-124](ISSUES.md)) — zero refusals — after two recipe defects the real run found were fixed ([ART-111](ISSUES.md), [ART-112](ISSUES.md)); the fix itself has no repo-side regression test, since proving it needs the user's own copyrighted media, which the no-content rule forbids — the real-media test hook is what stands in its place |
| OS install: build a distribution tree from the user's own AmigaOS 3.9 media (CD) | SD-2 G5 | ✅ | `core/osinstall/source_cd.rs` — a second `MediaSource`, reading an ISO9660 disc through `core/iso` rather than an ADF, discovered by `scan::find_media`/`scan::identify` alongside floppy images in the same media folder; `core/osinstall/recipes/amigaos-3.9.json` ships one component, `workbench-base`. Chosen at the request level by a release picker (`recipe::by_release`, an unknown name refused rather than defaulted, `InstallRequest.release` wired through to it; the remembered default stays "AmigaOS 3.2" so no existing setup changed meaning the day a second recipe arrived), and reachable by dropping a disc on the panel, which now offers the OS Builder (`os.install-from-disc`, priority 20 behind `iso.browse`'s 10, handed the dropped file's parent folder via `src/lib/hostPath.ts::hostParentDir`). **Run for real against the owner's own 469 MiB `AmigaOS39.iso`**: the recipe's 14 `from` paths were written in the mixed case every synthetic Joliet fixture uses, but this disc carries no Joliet descriptor at all — only a Primary tree, which ISO9660 keeps upper-case — so the first real run refused everything; corrected, `plan()` resolves clean (0 refusals, 663 items, 6,108,319 planned bytes). `apply()` could not open a CD at all ([ART-153](ISSUES.md), fixed) and was hashing the whole 469 MB medium into memory just to record its SHA-256 ([ART-154](ISSUES.md), fixed — `core/hashing.rs::sha256_file` streams instead). The re-run then failed on real disc content `apply()` could not write as literal Windows path segments ([ART-155](ISSUES.md), fixed) — filed first with a diagnosis that was half wrong (a reserved device name, `Storage/DOSDrivers/AUX`, measured directly on this machine to write and read back fine) and corrected by measurement to the real cause: three accented `.country` filenames (`Ö`/`Ë`/`Ñ`) that `core/iso/descriptor.rs::decode_iso646` was rendering as `?`, a character Windows refuses in a path. `decode_iso646` now decodes Latin-1 (AmigaDOS's own native charset) instead of substituting `?`. **The tree now builds end to end**: 588 files, 75 directories, all 663 planned items, from the real disc, in 6.2 seconds in a release build (the 20 seconds first recorded was a debug build) (`core::osinstall::apply::tests::build_the_real_39_tree_when_asked`, `#[ignore]`d, env-gated on `ART_OS39_ISO`/`ART_OS39_DEST`). `plan()`'s predicted `total_bytes` (6,108,319) still overstates what actually lands (6,054,225) by exactly 54,094 bytes — a CD-sourced directory's own ISO9660 extent length counted as if it were file content — open, not fixed ([ART-156](ISSUES.md)). **The bar is met: the tree boots.** Mounted as a `filesystem2=` directory volume under WinUAE with the owner's licensed Kickstart 3.1 (`amiga-os-310-a1200.rom`, V40) on an A1200/AGA profile, it reaches a clean Workbench with no error requester — `SetPatch`, `BindDrivers`, `AddDataTypes` and `IPrefs` all run. The screen's copyright line reads *1985-2000 Amiga International*, which is the 3.5/3.9 Workbench string and not the ROM's own (1985-1993) — **which proves only that the screen came from the tree rather than the ROM, and is equally true of AmigaOS 3.5**. **That is where this row was wrong, and the correction is the most useful thing on it.** Later the same day the running system was asked its own version instead of having it inferred from a copyright line, and it answered **`Kickstart 40.68, Workbench 44.5 (18-Aug-00)`** — which is AmigaOS **3.5** (the published AmigaOS version history gives 44.x for 3.5 and 45.1 for 3.9), and its Startup-Sequence failed on its first command, `C:LoadMonDrvs: Unknown command`. The disc carries **two** sibling trees under `OS-Version3.9` and the recipe placed only the first: `Workbench3.5` (673 rows) and `Workbench3.9` (854 rows, no `L`/`Expansion`/`Rexxc`/`T`, no `Startup-Sequence`, no `C/Version`, and a `workbench.library` of 199,852 bytes against 3.5's 193,400) — an **overlay**, which is why a 3.9 CD carries a `Workbench3.5` drawer at all. The recipe was never pointing at the wrong tree; it stopped after the first layer ([ART-169](ISSUES.md), fixed). A second component, `workbench-39`, declared **last** and `overrides: ["workbench-base", "locale-base"]`, lays the overlay over the base: tree 1257 → **1879 files**, 156 → **181 drawers**, 10,003,017 → **18,813,726 bytes**, and the classification of what it lands on is `upgrade=19 downgrade=0 same-version=0 unversioned=21 identical=130 new=622` (622 + 130 + 19 + 21 = 792, every overlay file accounted for). The booted system then answers **`Kickstart 40.68, Workbench 45.1 (13-Nov-00)`**, `LoadMonDrvs` is gone from the console, and the icons are the real 3.9 ones. The fix is pinned by `recipe::tests::the_39_overlay_is_declared_last_required_and_over_both_layers`, mutation-checked (dropping an override, reordering the components array and deleting a rule each fail it) — because the invariant test that looked like protection filters to `RuleKind::File` and every 3.9 rule is a `Subtree`, so it passed **vacuously** and reordering the array would have reverted the fix with nothing but a boot to notice. **And the sharpest lesson of the whole exercise:** `Libs/WORKBENCH.LIBRARY 193,400 → 199,852` is one of the 21 `Unversioned` rows — it carries no `$VER:` marker, so **the boot proved the decisive change and ART's own collision classifier could not**; any future "did the update take?" check built on collision classes alone will be confidently wrong about the one file that matters ([ART-170](ISSUES.md)). The upper-case ISO9660 filenames do not stop it: AmigaDOS is case-insensitive, which was expected and is now measured. The config is the one ART itself writes — `core::winuae::generate_uae_config`, not a hand-typed `.uae` — through `core::winuae::real_boot_hook::boot_a_distribution_tree_when_asked`, `#[ignore]`d and env-gated on `ART_BOOT_TREE`/`ART_BOOT_ROM`/`ART_WINUAE`. Notably it boots **without** the `First-Install`/`SetPatch` tree [ART-159](ISSUES.md) says is unplaced, so that hazard is real but not fatal to a base install. The Kickstart minimum 3.9 needs (V40 or newer) **is** stated: the recipe format gained `rom-at-least` beside `rom-older-than` ([ART-157](ISSUES.md), fixed), and `amigaos-3.9.json` uses it — `{ "condition": "rom-at-least", "major": 40 }`. It was filed because the format had only a maximum and the release's real requirement went unchecked. A whole-branch review then found the picker changed the *plan* but not the *checklist*: `OsInstall.tsx` rendered a hardcoded AmigaOS 3.2 catalogue whatever release was chosen, so 3.9 showed 26 components for a one-component recipe and labelled its `workbench-base` "Workbench3.2" — fixed by making the list a projection of the chosen release's own recipe (`osinstall_components`, a fifth read-only command; `AMIGAOS_32_COMPONENTS` deleted), with picks remembered per release so switching does not destroy the other's. The same review found `plan::relative_to` stripping a rule's `from` case-sensitively while resolution is case-insensitive — on a Joliet-pressed disc the strip failed silently and the whole tree landed nested under itself with no refusal — and a third `MediaSource` divergence (`CdSource::walk` on a file answered `Ok(vec![])` where `AdfSource::walk` refuses). Both fixed; `core/osinstall/source_contract.rs` now asks every implementation the same ten questions so a fourth divergence fails a test. Driven by hand in the running application, which immediately found two things no test had: a refusal that protected data and said nothing — three consecutive `ART-SAFETY-REFUSED` entries in the operation log over an existing destination, silent on screen because a failed job never emits `osinstall-result` — and a verify field labelled "Amiga volume image" with nothing to say what it meant. Both fixed: a failed or cancelled job now speaks where the button is, an occupied destination blocks *before* the button through the read-only `osinstall_destination_taken`, and the verify fields explain themselves. Two of the spec's own predicted hazards — `SetPatch`/the boot sequence and the language-variant trees — went untouched and are now filed ([ART-159](ISSUES.md)) |
| OS install: add an update package onto a built tree | SD-2 G5 (content layer) | 🟡 | **The engine is built and it works on synthetic material; against the owner's own three packages, none reaches the tree correctly.** What exists: a package archive is a **third `MediaSource`** (`core/osinstall/source_archive.rs::ArchiveSource` — LHA, ZIP or 7z, read through `core/archive`'s one security gate, `safe_join` between an entry name and any destination), and `open_nested` reads a payload archive **out of its wrapper** (a BoingBag's `member: "AmigaOS-Update"`), the inner bytes bounded by the outer gate. The nested extraction's temp file is created with `File::create_new` and a bounded retry rather than a timestamped name — found as a "flaky test" and correctly re-diagnosed as the production bug in miniature, since every real BoingBag's member is called exactly `AmigaOS-Update`, so two concurrent extractions collapsed onto one path (concurrent two-thread test at `source_archive.rs`). **Directories are synthesised at every level from the paths that imply them**: the real `BoingBag39-1.lha` payload declares 23 explicit directory entries, all second-level or deeper, and **zero** top-level ones, while every rule in both BoingBag recipes names a top-level drawer — so without this, every rule of both packages would have been refused `MediaPathMissing` against real material, and the engine's own fixtures had hidden it by declaring an explicit `TestPack/C/` row (the fixture was more helpful than reality; both fixtures now declare none, and stubbing the synthesis out fails nine `core::osinstall` tests). Archives are found by `scan::find_packages` in their **own** folder (`InstallRequest.package_folder`), kept apart from `find_media` so a 469 MiB disc sitting beside the packages is never opened to learn it is not one — **measured on the owner's real 58-item folder: 27 archives identified in 0.30 s**, a 171 MB `.rar` and a 248 MB `.7z` among the entries, nothing decompressed (`core::detect` reads a header window; `entries()` parses only the central directory). Three curated recipes ship as **data**, `core/osinstall/recipes/packages/{boingbag-39-1,boingbag-39-2,locale-turkish}.json`, each measured off the real archive rather than copied from a design sketch — 39-2's payload top level is *not* 39-1's (it gains `Rexxc`, loses `Fonts` and `Storage`), so a copied list would have named paths the archive does not hold. **Nothing is overwritten silently**: `core/amigaver.rs` reads a file's own `$VER:` marker (`compare_version`, deliberately neither `PartialOrd` nor `Ord` — the derived `PartialEq` compares the name and the comparison does not, and an operator that means less than it looks like is the trap itself), and `core/osinstall/collide.rs` sorts every overwrite into **five** classes — `Identical` (never listed), `Upgrade`, `Downgrade`, `SameVersion`, `Unversioned` — plus a `declared` column read from the package's own `overrides`. Two rules were added by construction rather than by reasoning, each after an *older* file was made to render as an upgrade: version-marker **names** are compared before numbers, and **the marker must name the file it was found in** (matched case-insensitively against the file's name, its stem, or the parent drawer joined to either — a font is `Fonts/courier/11` and calls itself `courier11`). Both were measured on the real tree before being adopted: **181 of 588 files carry a marker (31%)**, and **173 of 180 markers name their own file (96%)**; among the seven dropped is `L/Queue-Handler`, whose first marker reads `Version` — the bypass occurring in real material, caught by the rule. **Two entry points, one placer, and a test that binds them**: `produce(base + A + B) == add(produce(base + A), B)` byte for byte (`apply::tests::producing_with_a_package_equals_adding_it_afterwards` and `…producing_with_two_packages_equals_adding_the_second_afterwards`; the implementer sabotaged `add_package` and watched it fail before trusting it). That test is necessary and **not sufficient**, which the round measured rather than argued: with the undeclared-overwrite refusal stubbed out, only the two dedicated refusal tests fail and **both equivalence tests still pass** — two paths can agree on being wrong. Two defects it could not see were found by asking directly: `add_package` ran no collision detection at all (a package overwriting a base file without declaring it was refused by Produce and written silently by Add), and an overwritten file kept the **previous** file's `.uaem` sidecar, so protection bits would have reached a real Amiga volume wrong. Both fixed. And a third, found by asking whether the new refusal could fire on ART's own shipped packages: destinations were compared with `==` while every resolution around them is case-insensitive ([ART-012](ISSUES.md)'s rule) — the Joliet-less `AmigaOS39.iso` yields `C/ASSIGN` and a BoingBag's ZIP payload yields `C/Assign`, so **all ~211 files collided in reality and not in the comparison**: Add refused all 211 despite a declared `overrides`, and Produce wrote silently and left `distribution.json` naming a file whose SHA-256 matched nothing on disk. Normalised once, in `destination_key`/`same_destination`, used at 25 sites, with the rule's doc comment tied to `hash::name_hash` — the fold AmigaDOS itself uses. The fourth comparison site was found **inside `collide::preview`**, the function whose whole job is to warn about it. Tests: **262 `core::osinstall` + 41 `commands::osinstall`** (plus 9 `#[ignore]`d real-material hooks), of which 10 `core::amigaver`, 19 `collide`, 22 `source_archive`, 15 `package` and 14 `source_contract` — the last two questions added by the final whole-branch review, which found a fourth and a fifth `MediaSource` divergence the same way the first three were found, by a human reading files side by side. The contract now runs over **five** sources, not three: `ArchiveSource` appears once per archive format, because every archive fixture on the branch had been a ZIP while two of the three shipped recipes name a `.lha` — which is how [ART-168](ISSUES.md) survived 1875 tests. **What it does not do, as a boundary and not an oversight** — see the row's three issues: both BoingBag payloads are ZipCrypto-**password-encrypted** and the password belongs to the package's own Amiga-side `Updater`, so neither can place a single file ([ART-166](ISSUES.md), and the owner's recorded decision is that **no bypass will be written**); eight of the owner's archives claim the top-level name `LocaleUpdate` and two claim `BoingBag3.9-2`, so two of the three shipped packages cannot be resolved by name ([ART-167](ISSUES.md)); and the Turkish pack writes its 36 catalogs into `t<U+FFFD>rk<U+FFFD>e` rather than `türkçe`, reporting a clean install by every number the round measures while the booted Amiga lists 20 drawers where the host holds 21 ([ART-168](ISSUES.md)). **The round's own closing bar was not met, and was substituted** — said plainly here because the substitution is reasonable and being silent about it is not (final whole-branch review, m3). The design spec §7 set it as *"a 3.9 tree with BoingBag 2 applied reports a different Workbench version than a base one; booting it under WinUAE … is what closes this round. A tree that builds and does not boot is not a distribution."* No BoingBag file reached a tree *by this route* ([ART-166](ISSUES.md) — and none ever will, the payload being password-encrypted), so what actually closed the round was the **3.9 overlay** boot recorded in the row above. The spec's own bar was met on 2026-08-21 by the Amiga-side round instead: both BoingBags installed and the tree answered `Workbench 45.3`, with every file written by the package's own `Updater` inside the emulator rather than by `apply` — a different and genuinely valuable result, reached with the same method, and not the one the spec asked for. Nothing here claims a BoingBag booted; what was missing was the sentence saying the bar moved. Two of the spec's own §8 hazards went the same way and are now filed rather than merely unexercised: `WBStartup`/`Devs` arriving on a tree for the first time ([ART-171](ISSUES.md)) and a language pack colliding with the base `Locale` ([ART-172](ISSUES.md)) — the latter looked exercised, reporting 0 rows of every collision class, and was measuring a mangled drawer name. A package whose installation is an **Amiga Installer script** is refused with its reason, permanently and by design (spec §5), and **inspect-and-propose is not in this round** — ART supports exactly the three packages it ships recipes for, out of the 58 items in the owner's folder, and the screen says so rather than appearing to accept any archive |
| Update packages panel (`/os-builder`, Install kind) | SD-2 G5 (content layer) | 🟡 | `src/components/osbuilder/PackagePanel.tsx` + three read-only/mutating commands (`osinstall_packages`, `osinstall_collisions`, `osinstall_add_package`) mirrored in `src/lib/osinstall.ts`; 16 jsdom tests (`PackagePanel.test.tsx`), both i18n catalogues complete, the wire pinned in both directions. **One confirmation for the whole set**, not one per file — a BoingBag replaces hundreds of files and asking each time trains the user to click through. A **downgrade is marked as one by more than colour**: its own key wording, a "Downgrades (n)" heading placed first, and a badge shape distinct from every other group, so it survives colour blindness and a screenshot — the failure the preview exists to prevent, and thirteen of `ModulesA1200_3.2.adf`'s fourteen commands are why. All six new `RefusalReason` variants (`package-unknown`, `package-folder-missing`, `package-requirement-missing`, `package-component-missing`, `package-archive-missing`, `package-archive-ambiguous`) cross the wire **typed** and render as real sentences in both languages; they had shipped as `format!("{r:?}")`, so every sentence written into both catalogues was dead code and the user saw Rust debug output. `Collision::SameVersion`/`::Unversioned` were serialising their fields in the wrong case — `#[serde(rename_all)]` does **not** cascade to a struct variant's fields — found by probing the wire rather than by reading, fixed with explicit renames and pinned. The extraction a preview needs runs as a **job**, with a bounded result cache (64 entries, oldest evicted), a file/byte ceiling and a sweep of crash-leftover scratch directories — not synchronously on the command thread once per toggle, which is §54's rule broken in the place §54 was written for. `treeRoot`, `packageFolder` and the ticks are remembered — and since 2026-08-22 they are the **session's**, not this panel's ([ART-197](ISSUES.md#fixed)). They used to be deliberately not auto-filled from a just-finished build, on the argument that overwriting a remembered pick is the one outcome CLAUDE.md's own rule forbids; that argument was answered rather than dropped. Pressing Build is the user acting, so a finished install writes `tree.root` — and the result card **says which folder the next steps will act on** instead of the screen changing under the reader. The panel is now mounted at `/os-builder/paketler` and reads the session; opened without a tree it asks rather than rendering an empty card. A remembered pick whose archive has since vanished renders with a reason and **its checkbox stays live so it can be unticked** — it used to render checked *and* disabled, a dead end with no way out. **A package ART cannot place is no longer offered as though it can** (final whole-branch review, M3): both BoingBags declare `host_placement_block: "encrypted-payload"` in their own recipe JSON, so the row is untickable, carries its own badge, and says — in both catalogues — that the files are locked and only the package's own Amiga-side `Updater` has the key. Deliberately **not** folded into `available`, which means "your archive is not in this folder": saying "Archive not found" about a file sitting right there is the wrong sentence. A pick remembered from an earlier run can still arrive checked, so the preview is suppressed and Add is disabled for that selection too, while the row stays tickable *off*; `plan()` refuses it by type (`PackageNotPlaceableOnHost`) before the package folder is scanned and `osinstall_collisions` before an archive is opened, so a caller reaching the commands directly gets the same answer rather than the ZIP reader's. **And an entry name `safe_join` refused is now shown**, on the package's own row before the tick — `ArchiveSource` had collected them since Task 6 and displayed them nowhere, so an archive carrying `../../Startup` read on screen as an ordinary package (m6). **Never driven in `pnpm tauri dev` against real material** — every claim above is jsdom or unit-level, and this project's own record says that is where the six defects the owner found by hand were hiding |
| OS install: run a package's own installer on the Amiga | SD-2 G5 (Amiga-side round) | ✅ | `core/amigainstall/{mod,workvol,packagevol,stage,run}.rs` + `commands/amigainstall.rs`. ART mounts **three** volumes under WinUAE — the tree **copy** as data, the package's unpacked wrapper as data (`ARTPkg`), and its own work volume as the boot device at the highest `bootpri` — boots one generated `Startup-Sequence`, runs the installer the package's recipe names, polls the one-word result the Amiga writes, terminates the emulator it started, and promotes the copy over the user's tree **only** on success (§92). **Nothing decrypts anything and no protection is bypassed**; the wrapper is plain LHA, the payload stays encrypted, and the package's own `Updater` is what opens it. **It installs both of the owner's own BoingBags, measured end to end on 2026-08-21.** Through ART's own `compose` → `install` path, against fresh copies of their real 3 795-file AmigaOS 3.9 tree, their licensed Kickstart 40.68 and their own `AmigaOS39.iso`: `BoingBag39-1 (1).lha` (`Updater` 45.15) in **169.1 s** — `Succeeded`, `Promoted`, tree 3 795 → **3 859 files / 20 135 997 bytes** — and then `BoingBag39-2.lha` (`Updater` 45.19) **on that result** in **138.1 s**, `Succeeded`, `Promoted`, 3 868 files / 20 533 434 bytes. The result was then **booted and asked**, not inferred: `Workbench 45.3 (07-Dec-01)`, `version.library 45.3`, `workbench.library 45.127`, `resource.library 44.103`, where the same tree answered `Workbench 45.1 (13-Nov-00)` before. That is the round's own bar — *a BoingBag'd tree boots and shows its update* — met, and the deadline constant now carries the multiple §6 asked for (30 minutes = ten times the longest measured successful install, replacing a 60-minute guess). **Getting there took six rounds of measurement and one line.** For three sittings the `Updater` started, opened nothing, printed nothing, wrote nothing and never returned; the CD it verifies was mounted correctly and changed nothing; a 65 536-byte stack changed nothing; `LOCALE:` and the rest of the tree's assigns changed nothing; `IPrefs`/`ConClip`/`LoadWB` changed nothing. What it was missing was the tree's own `C:AddDataTypes REFRESH QUIET` — `datatypes.library` had an empty descriptor list, a state a booted 3.9 is never in — and that line **alone**, with nothing else added, produced a byte-identical successful install ([ART-193](ISSUES.md), with the six-row bisection table). **Nothing decrypts anything and no password is bypassed**: the package's own `Updater` opens its own payload, inside the emulator, which is how every established distribution builder installs a BoingBag. **Both refusal paths still refuse and copy nothing**, re-run the same day: `BoingBag39-1.lha` alone (stock `Updater` 45.13) in 3.6 s naming the archive to supply, and a required disc absent in 2.3 ms naming the disc and the volume — and that second sentence is now true by measurement rather than by inference, because with the fix in place and no disc the `Updater` **does** reach its own *"Checking AmigaOS 3.9 CD-ROM…"* line and stops on an AmigaDOS `Please insert volume AmigaOS3.9` requester nobody can answer (screenshotted). **What it still does not do**: no run has ever been launched from the panel (row below), and `apply` still places no BoingBag file — the Amiga's installer writes them ([ART-171](ISSUES.md)). **Five defects the 116 unit tests could not see were found in the first sitting**, three of which would have made ART say a confidently wrong sentence: a return code of 900 aborted ART's own script before it could report, so a refusal was on its way to being called a timeout ([ART-188](ISSUES.md)); the re-run guard read a marker written above `SetPatch`, and `SetPatch` **reboots**, so the installer was never invoked at all and that was heading for a timeout too ([ART-190](ISSUES.md)); `LIBS:` carried only the tree's `Libs`, so `resource.library` could not initialise and the installer refused to start ([ART-191](ISSUES.md)); no `ENV:` was built, so it stopped on a System Request nobody could answer ([ART-192](ISSUES.md)); and the tree's own `SetPatch` was never run ([ART-189](ISSUES.md)). All fixed and pinned by the exact-match script test. Tests: 121 `core::amigainstall` + `commands::amigainstall`, plus the two gated `#[ignore]`d real-material hooks that produced every number above |
| Amiga-side install panel (`/os-builder`) | SD-2 G5 (Amiga-side round) | 🟡 | `src/components/osbuilder/AmigaInstallPanel.tsx`, beside `PackagePanel` in the OS Builder, over `amiga_install_preview`/`amiga_install_run` and `src/lib/amigainstall.ts`; 21 jsdom tests plus 11 unit tests over the pure mappers, both i18n catalogues complete. §92's shape: read-only preview → confirmation → job → report. **The four endings are four sentences and four next steps** (succeeded / the installer refused / the deadline expired / the owner closed the window), asserted distinct on screen and not merely in the mapper — the round's signature defect is a confidently wrong sentence, and collapsing two endings is how one is produced. **Refusals are Rust's own English, verbatim** (ART-060): the prerequisite refusal names which package goes on first and in what order, the ART-186 refusal names the archive that fixes a 45.13 `Updater`, and a translated line adds the half the English cannot — that nothing was copied. `declaredOverlays` and `minimumInstallerVersion` are rendered **before** the run, so a refusal fixable with one download is never a dead end; a run that did not succeed names where the copy is, and a promotion that could not delete the tree it replaced names that too. The emulator window is announced before it opens, in both UX modes; beginner mode hides only the AmigaDOS command line, the three volume names, the result file and the machine, and disables nothing. Which packages are offered comes from the recipes (`PackageSummary.amiga_installable`), never a list of ids on the frontend. **Fix round 1** closed the one channel where Rust said something true and the screen dropped it: a run that goes wrong *mid-flight* leaves the copy on disk by design and `perform` reports where, as a `job-progress` message the panel never rendered — so the single path that really orphans a copy was the single path that said nothing about it. Rendered now, beside the error. The same question asked of every other channel found a second instance that was not silent but **wrong** (a cancelled run whose discard failed was answered with “the copy has been discarded”) and a third that was merely wasteful (the running phase note). **Amber for one reason now, and it is the original one**: no run has ever been launched from **this screen** — every real run on 2026-08-21, including the two that installed both BoingBags end to end, went through the engine's own gated hook, so the panel's rendering of a real refusal, a real progress stream and a real ending is still untested against anything but fixtures. The other half of the debt is paid: the engine now installs a BoingBag ([ART-193](ISSUES.md)) and the booted result answers `Workbench 45.3`, so there is at last something real for this screen to show — what is owed is driving it. The screen gained one field on 2026-08-21 — the disc a package's installer verifies, picked by the user, remembered like every other choice, with the volume the **image itself states** shown beside it in the preview and Rust's own refusal naming the disc and volume when one is required and absent |
| OS install: put the tree onto a PFS3/FFS volume | SD-2 G5 | 🟡 | `core/preload::NativeFormatter::copy_in` (`format_partition` then `copy_in`, the same two calls G5 always makes), reached through `commands/preload.rs::run_with_fallback` — native first, `hst-imager` only for the two known gaps ([ART-120](ISSUES.md)). **Re-measured 2026-08-16 against the real tree, through the path that now ships** — which replaces the earlier 3061-of-4030 / 969-excluded figures, a one-off manual pass against code that has since changed. The tree at `E:\amiga\ProjeART\dist-3.2` holds **3933 files / 280 directories / 12.2 MB** (`apply()` announced 4030/330; that number counts plan items, not entries — [ART-124](ISSUES.md)). Carried through the fallback, **all of it lands**: `hst-imager fs dir -r` counts *280 directories, 3933 files, 12.2 MB* on the volume, `Locale/Countries` holding `españa.country`, `österreich.country` and `türkiye.country` by name — so the non-ASCII quarter that [ART-113](ISSUES.md) excludes from the native writer is not lost, it is copied by the fallback. **And it boots**: the tree built for a V47 A1200 ROM (4047 files / 328 directories, `backdrops` included) boots AmigaOS 3.2 to a clean Workbench under WinUAE with the user's own licensed ROM — after [ART-126](ISSUES.md) (the RDB's `PatchFlags`) and [ART-127](ISSUES.md) (`icon.library`, `workbench.library`, and the wallpaper path the running system named itself). It lands in **one run, unattended**, which it did not at first: `hst-imager`'s first write into a volume `NativeFormatter` had just formatted died `ERROR_DISK_FULL` ([ART-122](ISSUES.md) — the two implementations size the PFS3 reserved area differently, and ART's number is `pfs3aio`'s own, so the fix was to stop mixing them). A partition is now formatted and filled by **one** tool, chosen before the destructive step. Reproducible, unlike the measurement it replaces: `commands::preload::tests::carry_the_real_dist_tree_through_the_fallback_path_when_asked` (`#[ignore]`d, env-gated). Which tool ran which step, and why, is reported per step (`StepReport`), never silently; when the fallback is needed and no `hst.imager.exe` is configured, the refusal names both the missing tool and the reason. The osinstall→preload handoff is still two separate manual steps, not one flow |
| OS install: verify a volume against its manifest | SD-2 G5 | ✅ | `core/osinstall/verify.rs` / `osinstall_verify` — presence, size and protection per file (content too, on PFS3), `NotChecked` kept apart from `Pass`/`Fail` (§89: "ART did not look" is never "ART found nothing wrong"), an unreadable filesystem family answering `NotChecked` for the whole run rather than failing it |
| OS install screen (`/os-builder`, Install kind) | SD-2 G5 | 🟡 | `src/components/osbuilder/OsInstall.tsx` / `src/lib/osinstall.ts` — 26 pure-logic unit tests, both i18n catalogues complete, the wire pinned in both directions (`commands/osinstall.rs`'s `wire_shapes` tests). Confirmed rendering in a headless browser: the route, the screen's own headings, five `h2`s resolved with no raw key and no `{{…}}`. **2026-08-19**: `OsInstall.test.tsx` (jsdom, 5 tests) confirms the real component mounts past its headings with the media/ROM/destination fields, the 26-entry checklist and the Build/Verify actions present; ticking a checklist component reaches the request `osinstallPlan` is asked to plan and changes the plan section shown; a refusal renders as a real translated sentence; and the whole tree carries no raw key or `{{…}}` in **both English and Turkish** — the checklist, its reasons and the refusals card are no longer unconfirmed. **Still unconfirmed**: the confirmation panel and file list in detail, the Verify states, and anything about layout or overflow (jsdom does no layout) — and the renderer crash itself: a real browser session still crashes reproducibly on this screen ([ART-118](ISSUES.md), narrowed not closed). **2026-08-22**: the screen is no longer one scrolling column. `/os-builder` is a shell — a progress strip showing only the steps the chosen kind has, over six sub-routes — and this component is the `kaynak` step; `PackagePanel` and `AmigaInstallPanel` moved to steps of their own instead of being stacked beneath it. 7 routing tests (`src/pages/osbuilder/steps.test.tsx`) cover a step opened cold, the strip's per-kind contents and a dropped disc reaching the media step. **The strip has now been measured in a real browser** (`scripts/osbuilder-strip-check.py`, headless Chrome over `pnpm dev`, 1280×900): it renders, the per-kind contents are right (boot-card 2, prepare-volumes 2, install 4), no label is a raw key or an unrendered `{{…}}`, and **English and Turkish measure identically** — 49 px tall at Application Size 1, 62.8 at 1.3, 186 at 2, with **zero** strip overflow and zero page horizontal scroll at all three. It wraps rather than clipping. **What is still unproven is everything a panel does**: the browser harness has no Tauri bridge, so no step has been driven end to end against real material. **And the reason it never had been is now known**: [ART-203](ISSUES.md) — `apply` refused every destination that existed while a folder picker can only return one that does, so **no distribution tree has ever been built from this screen**. Fixed the same day; a new or empty folder is now accepted and only a folder with something in it refused. The engine itself was proved on the owner's own `AmigaOS39.iso` that evening — 1242 files, 105 directories, 14.9 MB, 22.79 s, manifest written — but **through the engine's own `#[ignore]`d hook, not through this screen**, so this row stays amber and the first thing owed is a person building a tree from it |
| Raw device writes | §57 | ⏳ | deliberately absent until double-confirm UI exists |

### Collection & analysis

| Feature | Spec | State | Code |
|---|---|:---:|---|
| Folder scan into a title catalogue | §41, §42 | ✅ | `core/gameindex/scan.rs` (a job; `core/collection.rs` retired onto it) |
| A title's facts read from what states them | §41, §42 | ✅ | `core/gameindex/readers/` — a WHDLoad slave's `ws_name`/`ws_copy`/`ws_Flags`, an `.rp9` manifest, a bootable hardfile's insides, a TOSEC filename |
| Provenance carried beside every fact | §14, §34 | ✅ | `record::Fact<T>`; the screen marks anything guessed rather than read |
| Multi-disk grouping | §41 | ✅ | `readers/tosec.rs` (`disk`), `record::Media::Floppies` (`.rp9` states the order) |
| Cover art / screenshots in the listing | §41 | 🟡 | `core/artwork/` — two online sources, plus `core/artwork/local.rs`'s offline pass: extracts an `.rp9`'s own embedded `screen-running` PNG straight from the package, no network, no consent question, adopts rather than re-extracts on a second run, refuses a `../../evil.png` entry. Stores under the same normalised key `artwork_known` reads — a Task 6 review caught it storing under the raw catalogue title first, which would have sent every one of the 242 pictures to a key nothing ever looked up. **Run against the user's own folder on 2026-08-18: 242 written, 0 adopted, 0 missed** — every one of the 242 `.rp9` packages gave up its picture on the first pass. That measures the extraction; whether the pictures *render* is the other half of the same seam and is confirmed separately on screen |
| Artwork sources configurable in Settings | §41 | ✅ | `core/artwork/config.rs` — source types are code, on/off and both mirror bases are the user's. Every source ships enabled; nothing fetches until the user starts the job |
| A hand-attached picture | collection-wave-c §2 | 🟡 | `ArtBinding { chosen, cached }` in `RecordOverride`'s user layer — the bytes go into the artwork cache under source `manual`, the choice lives where a refresh cannot touch it, and a later online fetch skips a title that already has one. PNG/JPEG only; IFF/ILBM is not decoded, by the wave's own design. **Deleting the artwork cache re-materialises the binding from `chosen`** (since [ART-143](ISSUES.md), fixed 2026-08-20) — `core/artwork/rebind.rs` reads the file the user originally picked and puts the picture back, on every catalogue load rather than behind a button, because the choice is already the user's and already recorded. It also covers the case that actually happens: an `index.json` left in place with the picture files deleted, which is a row pointing at nothing rather than a missing row. A job, since a full cache deletion is hundreds of reads and writes |
| The Collection's detail panel | collection-wave-c §3 | 🟡 | `src/pages/CollectionStudio.tsx`, `src/components/collection/TitleDetail.tsx`, pure logic in `src/lib/collectionDetail.ts` — opens from a card or a table row alike, shows the picture with a switch between the kinds a title has, the media spelled out (disk order, slave name, hardfile image), the declared Kickstart, the path, and the actions. Beginner mode hides only the path and block-level detail. The panel's open state and selected title are a plain `useState`, not remembered — the wave's own ruling that "which file the screen has open" is not a user setting, unlike everything else on the settings/remembered list. Frontend-tested; **never opened in `pnpm tauri dev`** |
| Play a catalogued title in WinUAE | collection-wave-c §4 | 🟡 | `core/launch/` (plan, `.rp9` disk/hardfile extraction, the Y2 boot directory) plus `core/winuae.rs`'s `DirMount` lines, reached from the panel's Play button. Picks a Kickstart from the ROM folder, **refusing rather than launching** when none suits. **Which machine depends on the shape, and since [ART-152](ISSUES.md#fixed) a WHDLoad launch no longer reads the catalogue at all**: every WHDLoad-shaped request gets one named profile, `AmigaProfile::whdload_a1200` (68EC020, AGA, 2 MB Chip, 8 MB Fast, Kickstart 3.x), because a WHDLoad title boots AmigaDOS and WHDLoad rather than the game the catalogue's chipset describes; floppies and plain hardfiles still follow the catalogue's chipset exactly as before, and the user's per-title machine override outranks both. The Fast RAM figure is a default and not a floor — a Settings value below the profile's own 8 MB is used as typed, 0 included. A bare `.adf`/`.rp9` floppy set mounts and boots directly, a plain hardfile mounts read-only, and — since [ART-147](ISSUES.md) — a *self-booting* WHDLoad hardfile (`Media::WhdloadHardfile`, most WHDLoad titles in a real collection) takes the same plain-hardfile path with no system volume and no boot directory, an off-by-default `allow_write` switch offered once the screen has said leaving it off means saves are lost. A WHDLoad **drawer** — an already-unpacked pack needing a separate bootable system — still gets Y1 (mount the user's own system read-only, hand over) with Y2 (ART's own one-click boot directory) on top, but nothing the catalogue reads produces that shape today; it is reached only by hand-built requests and its own tests. Every launch goes through the operation log. Findings from review, fixed before landing: an `.rp9`-packaged HARDFILE title was mounting the archive itself, writable ([ART-141](ISSUES.md)); a WHDLoad slave name was refused for quoting/redirection characters but not `;`, `` ` `` or `$`, all of which the AmigaShell command-line parser accepts; and [ART-147](ISSUES.md) itself, found by running Y2 against a real title (`1000 Miglia`) — the catalogue had called it a drawer needing a system it does not have or need, landing the user at a CLI after choosing an unrelated Workbench image as "the system." **Run for the first time 2026-08-18**: Y2 against `1000 Miglia` under the *old*, now-fixed classification landed the user at an AmigaDOS CLI for a different reason first ([ART-145](ISSUES.md), fixed same day). **And a WHDLoad game now runs for real, from this user's own collection**: after [ART-147](ISSUES.md), [ART-148](ISSUES.md), [ART-149](ISSUES.md) and [ART-151](ISSUES.md) landed, `1000 Miglia` (one of 1697 self-booting WHDLoad hardfiles catalogued the same way in this collection) was launched from the Collection screen and reached Simulmondo's own title logo in the WinUAE window — the self-booting `Media::WhdloadHardfile` path, end to end: classified correctly, mounted directly with no system volume, machine picked as A500/OCS **(that run predates [ART-152](ISSUES.md#fixed); see below for the same title re-run on the A1200 profile)**, Kickstart 3.1 chosen over the folder's older 1.3 by "highest major wins", bare-hardfile geometry `32,1,2,512`, 8 MB Fast RAM added for WHDLoad's own headroom, mounted `rw` on this title's own `allow_write` opt-in. One title proven; the other 1696 built to the same shape are a strong signal, not each individually proven. **The A1200 WHDLoad profile was driven from the panel on 2026-08-21, twice, and the second run is the one that settles something.** First `Akira` (ICE, 1994 — `AkiraCD32.slave`, `Akira v1.3 CD32.hdf`, catalogued **AGA (1200/4000)**): Play pressed on the Collection's detail panel, the confirmation screen reading `A1200 · Kickstart 40.68 (A1200) · 2048 KB Chip + 8 MB Fast` — the decided profile exactly, **read off the running application** — the A1200 Kickstart picked out of the owner's own ROM folder, and the game reaching its own menu. An AGA title on an AGA machine is the expected result and says nothing about routing an *OCS-only* title through this profile; it was nearly written down as if it did, and checking which of the two `Akira` hardfiles it was is what caught that. Then `1000 Miglia` (Simulmondo, 1992 — `1000 Miglia v1.2.hdf`), which does settle it, because ART had **already** played this exact file on the old A500/OCS/68000 path: re-launched from the panel with the A1200 profile in place, **it ran**. Same file, same ROM folder, only the machine profile changed — and it changed by itself, the picker left on `Otomatik`/Auto (`machine_override: null`), so `machine_for_request` produced the A1200 rather than anyone choosing it. The card showed this title's chipset as `Bilinmiyor`, so there was nothing in the catalogue to derive a machine from in any case. **One OCS-era title, launched and running, is the whole claim** (§10/§89) — not the other 1696 built to the same shape. `Akira`'s chipset badge read `~tahmin`: ART inferred it rather than reading it from something that states it, and said so, which is the Collection working as designed. **An `.rp9` floppy title runs for real too**: *3D Demo* launched on the same day and the game started — ART detected the package, extracted its two floppies **in order** into its own launch directory (`launch/3d-demo-491e8a16/3ddemo1.adf`, `3ddemo2.adf`, read off disk rather than reported), chose a machine and a Kickstart, wrote the configuration and started WinUAE. That is the plan, the extraction, the multi-disk order, the ROM choice and the launch, end to end. Still unrun: a bare `.adf` floppy set, an `.rp9`-packaged **hardfile** title (102 in this user's collection), the WHDLoad **drawer**-plus-system Y1/Y2 path, any VHD/RDB system image ([ART-146](ISSUES.md) remains unproven for exactly this reason), and whether a save survives with `allow_write` turned on — **narrowed by measurement rather than left blank**: two titles were played with the switch on and the images were read back off disk after the emulator closed, still carrying their original 2021 timestamps and byte counts, so the mount really is writable (the generated configuration says `rw`) but nothing in that session wrote. Likely the ordinary reason — an Amiga game writes its high-score table on a clean exit, and the window was closed rather than quit through WHDLoad's own key — so what is unproven is the game's write, not ART's mount |
| Chipset, genre and rating from an online source | §41 | ⏳ | **unsourceable today, not unimplemented.** Lemon Amiga returns 403 to every non-browser request including `robots.txt`; Hall of Light is HTML only and ART does not scrape (§41.5.3); OpenRetro holds exactly the chipset data and expects third-party apps but documents no endpoint. Measured: `chipset` is stated for 9.6 % of one real 1700-title catalogue and cannot be inferred — 83 records have a slave that left `ReqAGA` clear while the title says AGA or CD32 |
| Catalogue persistence, multiple folders | §41 | ✅ | `core/gameindex/store.rs` — one JSON per root, refreshed only when asked. A cached entry is reused when path, size and mtime match and the record's schema is current; a renamed file is followed by its content-derived id rather than duplicated. 27 tests, and the second Update of an unchanged folder was watched reading nothing on a real screen |
| Hand-edited titles | §41 | ✅ | the override layer, its backup policy and "the user always wins" were built in wave A; **the editing UI landed with the name tool** — every row has an Edit button, Enter saves, and the correction is a `UserEdit` (top provenance) that survives every refresh and undoes cleanly |
| Cleaning up hand-named files | §41 | ✅ | `core/gameindex/cleanup.rs` — ART **proposes and the user accepts**, never renames on its own ([ART-136](ISSUES.md)). Explicit disk markers are removed from a title and normalised in a filename; a bare trailing number is settled by the neighbours (`dune2-1` beside `dune2-2`) and only when the set begins at disk one, which is what keeps eighteen disk-magazine issues from collapsing into one. Renaming a real file confirms first, refuses an existing target, and is logged; the catalogue follows it by content id. 847 real files → 523 distinct titles |
| Tags, favourites | §41 | ⏳ | — |
| Duplicate detection (SHA256) | §43 | 🟡 | identical bytes already collapse to one record (`derive_id`); no dedupe *view* |
| Hex viewer (read-only) | §31 | ✅ | `core/analysis.rs` |
| Signature scanning | §28 | ✅ | `core/analysis.rs` |
| Disk Analyzer / forensics | §28 | 🟡 | hex + signatures only |
| Image compare | §27 | ⏳ | — |
| Binary / Hunk inspector | §30 | 🔩 | `core/binary.rs` |
| Recovery Lab | §29 | 🔩 | `core/recovery.rs` |
| Image conversion | §26 | 🔩 | `core/conversion.rs` |

### Application shell & UX

| Feature | Spec | State | Notes |
|---|---|:---:|---|
| Universal Drag & Drop | §4 | ✅ | one global listener, `lib/dnd.ts` |
| "What can I do?" panel | §46, §91 | ✅ | driven by the engine's plan |
| Dashboard, recent files | §62 | ✅ | |
| Settings (theme, paths, language) | §59 | ✅ | Every path has a Browse button beside the box (ART-086); the Aminet download folder is one of them |
| A scratch folder the user chooses | [ART-196](ISSUES.md#fixed) | ✅ | `src-tauri/src/scratch.rs` holds one root for everything ART stages and throws away — preview extractions, install staging, unpacked packages, a launch's `.rp9` disks, the emulator's own generated `.uae`, the scan cache, a selection copy's cache. Eighteen call sites reached `std::env::temp_dir()` on their own before this, which on Windows is the **system drive**, and the product could not honour the owner's standing rule that ART writes nothing to `C:`. **`core/` still does not choose it**: a `core/` function that stages takes the directory, and where threading it would have rippled through 86 call sites the bare name stayed as a thin wrapper with an explicit `*_staging_in` variant beside it — the `scan_collection_directory` / `..._with` split. **A chosen root that cannot be reached is a refusal, never a fallback**: `ART-SCRATCH-UNAVAILABLE` names the folder, the reason and that nothing in it was moved, and it is raised on the command thread *before* `spawn_job`, so the user hears it from the button they pressed. **Asked once on first run** (`ScratchRootGate`), with *use the system drive* as the answer the first button gives — the owner's own ruling, and asked here rather than by the installer because ART ships two bundles and first-run also covers running the executable unpacked. A Settings entry too, showing what the default actually resolves to rather than describing it. **Changing it moves and deletes nothing**, and names where the old root was. 14 Rust tests + 9 jsdom; eight mutations run and eight fell, with two survivors disclosed in the issue. `scripts/scratch-root-sweep.py` fails CI on the nineteenth call site. **Kept data is deliberately unmoved** — the operation log, catalogue, artwork and checkouts stay under Tauri's per-app directories, and downloads keep their own setting ([ART-214](ISSUES.md#fixed)). **Not driven in `pnpm tauri dev`** |
| Every choice remembered | user rule | ✅ | `src/lib/remembered.ts` — a persisted value is read back **through a guard**, so a stale or hand-edited settings file falls back to the default rather than putting a bad value on screen. `settingsStore` refuses to let a late-landing read overwrite a key the user has already touched this run (the other half of ART-089) |
| Application Size (Ctrl +/-/0) | §64 | ✅ | `src/lib/appZoom.ts` — 70–250 % in tens, set from Settings or the keyboard, remembered. Most of the people using ART are over fifty; this is a first-class setting, not an accessibility afterthought. The right-edge complaint ([ART-099](ISSUES.md#fixed)) **did not reproduce when measured**: seven screens at 100/130/200 % in a window the size of the one it was reported on, nothing overflowing (`scripts/zoom-check.py`). What was real is fixed — the content area can be scrolled sideways now instead of clipping in silence |
| About: author, licence, source | §67 | ✅ | Settings → About — the logo, name and version, author (tolon), the GitHub address, GPL-3.0-or-later with the warranty notice and where the licence text lives. No email anywhere, deliberately |
| i18n architecture | — | 🟡 | English and Turkish, 1776 keys each, chosen in Settings and remembered; `CoreError` and `WhdloadRefusal` sentences from Rust still reach the UI in English regardless of the chosen language ([ART-060](ISSUES.md#open)) |
| Dark / light theme | §61 | ✅ | |
| Beginner / Power User mode | §47, §48 | ✅ | `lib/uxmode.ts`; hides advanced studios, actions and block detail |
| Operation history + export | §53 | ✅ | Settings → Operation Log |
| Workflow Wizard | §45 | ⏳ | — |
| Two-pane file manager | §11, §17 | ✅ | `/files`, Total Commander-styled (row icons, file-type colour, Attr column); F3 view · F4 edit · F5 copy · **F6 move** (Shift+F6 rename) · F7 mkdir · F8 delete · F9 attributes, plus F2/Ctrl+R refresh. Any volume to any volume for a single entry; see Multi-select below for what a selection can and cannot do |
| Pane header: source combo + path + filter | Brief §1.3 | ✅ | `src/lib/paneSources.ts` (pure, 10 tests) — enumerated mounts from `panel_local_roots` plus the six picker sources, no hardcoded letters; the button strip behind it is `showSourceButtons` in Settings, default off |
| Command line (navigate + filter) | Brief §1.4 | ✅ | `src/lib/commandLine.ts` (pure, 8 tests) — a full path, `cd ..`, or a `*`/`?` mask. Running programs is deliberately out of scope (§56) and refused by name, never silently ignored |
| Move (F6) | Brief §1.4, §92 | 🟡 | `src/lib/movePlan.ts` (pure, 16 tests) + `moveSelection` in `FileManager.tsx`: copy → **re-list the destination and look for every moved name** → delete, so a stopped move can only leave a duplicate. A collision is refused, not resolved by the overwrite policy. The three refusals this row used to list are gone: a **host folder can be the source** ([ART-080](ISSUES.md#fixed), the delete going to the Windows Recycle Bin through `core/hostfs` + `tools/recycle_bin.rs`), and **any selection between two images** — several entries or one — goes through `volumeCopyBetweenMany` + `volumeDeleteMany`, one staged commit each ([ART-064](ISSUES.md#fixed), [ART-081](ISSUES.md#fixed)). What is still refused is refused on purpose and by name: a host **root** (`C:\`), host→host (not ART's job), a read-only or unwritable side, and same-image |
| Enter opens a container in the same pane | Brief §3.1 | ✅ | `src/lib/containerStep.ts` (10 tests) + `src/lib/paneHistory.ts` (15 tests). What a row opens into comes from `analyze_paths`, never the extension; `PaneState.host` carries the way back out, so `[..]` at a container's root returns to the host folder **with the cursor on the container file**. Verified on the running screen with a real ADF. Backspace / Ctrl+PgUp go up, Ctrl+PgDn enters, Alt+Left / Alt+Right are per-pane history with container steps as places |
| Full keyboard coverage | Brief §3.2 | 🟡 | Space marks in place, Insert marks and advances, Ctrl+A includes directories, the numpad marks by mask and inverts, type-to-search moves the cursor and only the cursor (`src/lib/quickSearch.ts`, 15 tests), F2/Ctrl+R refresh, Alt+F1/Alt+F2 open the source combos, Ctrl+B the sidebar. **Space computes a directory's size** ([ART-087](ISSUES.md), fixed 2026-08-20) — `core/dirsize.rs`, reached from both `panel_directory_size` and `volume_directory_size`, so a host folder and an Amiga volume are counted through the same primitive |
| Tabs per pane, and session restore | Brief §3.3 | 🟡 | `src/lib/paneTabs.ts` (19 tests): a tab is a `PaneLocation` plus its sort order and mask, so it can live inside an image. Ctrl+T duplicates, Ctrl+W closes (never the last), Ctrl+Tab cycles, middle-click closes. Persisted to the settings store and validated on the way back in (`src/lib/paneSession.ts`, 9 tests, including the JSON round trip a restart really performs). **The write half is verified on the running application** — two tabs made in the pane appear in `settings.json` as two tabs. **The read-back half has still not been seen**: nothing has closed and reopened ART since |
| Per-filetype colour rules | Brief Part 2 | ✅ | `src/lib/colourRules.ts` (11 tests): mask → colour, first match wins, several masks per rule separated by `;`. Three ART defaults — containers, archives, ROMs. Sits *in front of* the built-in classification, so an empty list changes nothing, and a malformed stored list falls back to the defaults |
| Multi-select (Shift/Ctrl-click, Insert, Ctrl+A) | Roadmap 1.1 | ✅ | `src/lib/selection.ts` (pure reducer); real pane focus (`focused: Side`) and Tab between panes. The count and its size live in each pane's own Total Commander status line |
| Batch copy: local ↔ volume ↔ volume | Roadmap 1.1 | ✅ | `commands/volume_write.rs` — all three directions are now one primitive each, one job, cancel commits nothing: `volume_plan_copy_many`/`volume_copy_in_many` (host→volume), `volume_extract_many` (volume→host — mounts once, walks once, one `ExtractReport`, replacing the per-entry `Promise.all` that left a failed seventh entry with six on disk and three never attempted, [ART-065](ISSUES.md#fixed)), and `volume_copy_between_many` (volume→volume — stages the whole selection into one temp folder and inserts it as one backup and one commit, replacing an outright refusal, [ART-064](ISSUES.md#fixed)) |
| Batch delete | Roadmap 1.1 | ✅ | `volume_delete_many`; a batch that can't fully succeed (missing name, non-empty directory) deletes nothing |
| Several archives installed to a disk at once | Roadmap 1.1 | ✅ | `archives_plan_install` / `archives_install` (`commands/archives.rs`); each archive gets its own drawer, staged into one write so a cancelled batch can't leave two games half-installed. Planning runs as a job with its own progress and Stop ([ART-066](ISSUES.md)), and Stop is answered *inside* one archive's extraction rather than only between two ([ART-067](ISSUES.md)) |
| One listing order + per-pane column sort | Roadmap 1.2 | ✅ | `commands/panel.rs`, `core/adf/fs.rs`, `core/volume/write/dir.rs::entries_in` now share one folders-first, case-insensitive floor; `src/lib/sort.ts` sorts on top of it by name/size/date, click-to-reverse |
| Filename mask filter | Roadmap 1.2 | ✅ | `src/lib/mask.ts` — Total Commander `*`/`?` wildcards, whole-name, case-insensitive; narrows what a pane shows and clears its selection on change. "this folder is empty" and "your mask matches nothing" are told apart by the matcher itself rather than inferred from two counts ([ART-068](ISSUES.md)) |
| Checkout / checkin (F4) | Stage W §6 | ✅ | `core/volume/checkout.rs`; SHA-256 gated, CRLF offered never applied |
| `.uaem` sidecars | Stage W §4.2 | ✅ | `core/volume/write/uaem.rs`, WinUAE's format; round-trip test pins `HSPARWED` |
| `.info` pairing | Stage W §7.1 | ✅ | Rename, delete and move all offer the icon; the copy plan warns when a pair is split |
| Attributes editor | Stage W §7.2 | ✅ | All eight bits with explanations; Power edits, Beginner reads |
| Explorer drag-out | §17, §90 | ✅ | `tauri-plugin-drag`, local files only |
| File associations | §59 | ⏳ | requires explicit consent flow |
| Accessibility (keyboard, focus, contrast) | §64 | 🟡 | not audited |

### Spec addenda

Both are designed — [design-software-sources.md](design-software-sources.md),
[design-ai-layer.md](design-ai-layer.md). Scheduling is in
[STATUS.md](STATUS.md#stage-plan).

| Feature | Spec | State |
|---|---|:---:|
| Aminet catalog sync / search / fetch | §41.5 | 🟡 |
| Aminet install to HDF | §41.5 | ✅ | `sources_install_volume` — same unpack as the ADF path, then a Stage W folder copy |
| Aminet update view | §41.5.6 | ✅ | `core/sources/installed.rs`; compares the recorded download against the catalogue |
| Package bundles — a curated catalogue, downloaded in order | [design-package-bundles.md](superpowers/specs/2026-08-22-package-bundles-design.md) | ✅ | 14 sets, 62 entries, shipped as data (`core/sources/bundle/catalogue/*.json`); `core/sources/bundle/` (parse/resolve/run) + `commands/bundles.rs` + a section inside Aminet Studio (`BundlePanel.tsx`). Phase 1 is download-only — nothing installs onto an Amiga volume yet |
| AI read-only assistant | §45.5 Stage A | ⏳ |
| AI plan generation + Plan Cards | §45.5 Stage B | ⏳ |
| AI full scenarios | §45.5 Stage C | ⏳ |

The Aminet rows are ✅ apart from the AI layer. What is built and tested:
`core/sources` — index and readme parsing, catalog store, search, version
resolution, mirror failover, the download trust pipeline and catalog sync (167
tests); `net/http_mirror.rs`, the `ureq`-backed transport (16 tests);
`commands/sources.rs` (12 tests); `src/lib/sources.ts` and Aminet Studio at
`/aminet`. Core tests run against an in-memory mirror and the transport tests
against a localhost socket, so CI never leaves the machine.

The index format, the mirror defaults and the whole pipeline were verified
against live Aminet mirrors on 2026-08-09 — which is how
[ART-030 and ART-031](ISSUES.md#software-sources-aminet-415) were found.

Since then: sorting (six orders) and filters (name-only, age, size range, file
type) in the catalogue; a **user-chosen download folder** with subfolders and
the ability to move a download afterwards; installing a downloaded package into
an ADF; and a two-pane file manager at `/files` with drag & drop.

Also since then: **"Show in Collection"** hands the download folder to the
Collection screen, which accepts a folder through router state the way the
other studios do; the **mirror list is editable** in Power User Mode — reorder,
edit, add, remove, and `sources_reset_mirrors` to go back to the shipped list;
and both the download folder and any custom mirror order are **remembered
across launches** (they lived only in process memory before, so every restart
silently reset them).

Stage A is complete. The **update view** compares each recorded download
against the catalogue as it stands now — Aminet's index has no version column,
so "newer" means the entry changed since the download, and the panel says which
signal fired rather than showing a bare badge. **Install to HDF** unpacks with
the same extractor the ADF path uses and then copies the unpacked folder in
with the Stage W writer: one install path, two destinations.

What remains under §41.5 is the AI layer (§45.5), which is designed
(`docs/design-ai-layer.md`) and not built.

Note on "Show in Collection": ART's Collection indexes a folder, it is not a
database rows are added to. The action therefore indexes the download folder
rather than pretending to file one package away somewhere — §41.5.6's intent,
honestly implemented against the Collection that exists.

**Package bundles**, added 2026-08-22: a curated catalogue of 62 Amiga
packages in 14 named sets — the Emu68 Imager's own 60-entry list, verified
per entry rather than transcribed, plus the owner's `tolunnet` and
`tolunwifi`. The user ticks sets (or the computed `hepsi` "everything") and
ART downloads every entry in the catalogue's own order, sequentially,
through the existing mirror engine and download trust pipeline — nothing
here opens a new kind of connection. `EntrySource` is a closed enum with no
URL variant (`aminet`, `aminet-search`, `github-release`, `mirror`,
`user-supplied`), so an entry ART cannot honestly fetch today — a
`user-supplied` file, an unconfigured `mirror`, every `aminet-search`
("latest version") and every `github-release` — is refused *before* the
tick with its own reason sentence, never offered a working-looking checkbox
(§10/§89); a set with zero fetchable entries, like the shipped `emu68` (4/4
`github-release`), cannot be ticked at all. Six distinct outcomes per entry
(`Downloaded`, `AlreadyHave`, `NotPlaced`, `Refused`, `Failed`, `Skipped`)
stay separate all the way to the screen, which lists every entry with the
string its own outcome carries — not just a count. Cancelling ends the job
`Cancelled`, never `Finished`, matching the report it already showed. A
permission-flagged entry (`⚠` in the design doc) shows its condition above
the tick, informational rather than a gate, and every one is bound to
`THIRD_PARTY_LICENSES.md` by a test. The ticked sets are remembered across
navigation, guarded, like every other choice in the product. `exclusiveGroup`
and `requires` are recorded on the data but not yet enforced — installing
onto an Amiga volume, where they will matter, is a separate design.

---

## Keeping this honest

When you finish a feature:

1. Move its row to ✅ **only** once a test exists.
2. If the UI can reach it, check the workflow catalogue
   (`core/workflow/builtin.rs`) — an action registered `available: false`
   should be flipped to `true` in the same change.
3. Update the format table if the change alters what ART can claim to support.
4. Add a line to the session log in [STATUS.md](STATUS.md).
