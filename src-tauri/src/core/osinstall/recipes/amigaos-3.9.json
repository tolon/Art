{
  "_why_no_T": [
    "JSON has no comment syntax and `Recipe` does not deny unknown fields, so this",
    "array is how this file records a decision a reader would otherwise have to guess",
    "at. It is data nothing reads.",
    "",
    "The disc's `OS-Version3.9/Workbench3.5` holds 16 top-level drawers; the rules",
    "below place 14. `T` and `WBStartup` are the two left out, and both are",
    "deliberate rather than overlooked:",
    "",
    "`T` is AmigaDOS's temporary directory, and on a running 3.9 system it is not",
    "on SYS: at all. Measured on the owner's own AmigaOS39.iso (2026-08-19): the",
    "disc's own `S/Startup-Sequence` — the file this recipe *does* place — runs",
    "`C:MakeDir RAM:T RAM:Clipboards RAM:ENV RAM:ENV/Sys` and then",
    "`Assign >NIL: T: RAM:T`, so `T:` is made in RAM at every boot and the disc's",
    "copy is never consulted. The disc's `T` drawer is also empty (0 entries, by",
    "7-Zip). AmigaOS 3.2's tree, which ART built and which booted to a clean",
    "Workbench under WinUAE, likewise carries no `T` and its own Startup-Sequence",
    "does the identical two lines. Copying an empty drawer whose assign points",
    "elsewhere would place a directory the system never looks at.",
    "",
    "`WBStartup` belongs to the second, unwritten half of this recipe rather than",
    "to `workbench-base`: it is where a system's own startup programs are chosen,",
    "and which ones a 3.9 tree should carry is one of the things the boot test",
    "(spec section 4) was meant to establish and did not, because no 3.9 tree has",
    "been booted yet.",
    "",
    "If a boot disagrees with either judgement, add the rule then, with the run as",
    "the reason — that is what a real run is for."
  ],
  "_why_locale_base_ART_162": [
    "Added in a Task 4 review fix round, not the round that first wrote this",
    "file — filed as ART-162. The disc carries a whole second top-level drawer,",
    "sibling to `Workbench3.5`, this recipe placed nothing from at all:",
    "`OS-Version3.9/Locale` (Catalogs, Countries, Flags, Help, Languages,",
    "Providers — measured with `7z l -slt` against the owner's own",
    "AmigaOS39.iso). Without it, `locale.library` has no `.language` or",
    "`.country` file to select *any* non-English locale by — not just Turkish,",
    "every one of them — which is what made the `locale-turkish` package (this",
    "same task) inert: its 36 catalogs would sit on the volume with nothing",
    "that could ever pick the language they translate.",
    "",
    "Four of the six drawers are taken: `Catalogs`, `Countries`, `Languages`,",
    "`Help` — the set `locale.library`'s own selection actually reads",
    "(Locale prefs picks a `.country`/`.language` pair and points programs at",
    "`Locale:Catalogs/<language>/*.catalog`). `Flags` (globe icons for the",
    "Locale prefs country picker) and `Providers` (dial-up ISP presets for",
    "Miami-era internet software this project does not install) are left out",
    "**of this component**, whose scope is locale *selection*.",
    "",
    "**Corrected in Task 8, fix round 2 (F3).** What stood here said they",
    "were left out as `cosmetic/unrelated`, which reads as a judgement that",
    "they are unwanted on the volume. They are not, and as of ART-169 they",
    "ship: `workbench-39` places `OS-VERSION3.9/WORKBENCH3.9/LOCALE` whole,",
    "and its 61 `Flags` rows (Countries **and** a whole `Keymaps` set, only",
    "16 of which this component's own source even has) and 4 `Providers`",
    "reach the volume through it. That is accepted and intended — both are",
    "part of AmigaOS 3.9. The exclusion here was always about *this*",
    "component's scope, never about the files, and the two components are",
    "complementary rather than overlapping on this point: `locale-base` is",
    "the only source of any catalog or `.language` on the disc, and",
    "`workbench-39` is the only source of `Flags` and `Providers`. See that",
    "component's own `_why_overrides_and_why_last` for the measured",
    "per-drawer comparison.",
    "",
    "`required: false`, matching the shipped AmigaOS 3.2 recipe's own",
    "`locale-base` component exactly (`recipes/amigaos-3.2.json`): a system",
    "with no Locale tree still boots and runs in English, the ROM's own",
    "default — nothing analogous to `install-libs`'s missing `icon.library`",
    "forces this one on."
  ],
  "release": "AmigaOS 3.9",
  "components": [
    {
      "id": "workbench-base",
      "label_key": "osinstall.components.name.os39.base",
      "media": "AmigaOS3.9",
      "required": true,
      "_why_rom_at_least_40": [
        "ART-157. The recipe format could state a Kickstart *maximum*",
        "(`rom-older-than`, which 3.2 uses to add `modules-a1200` back for a",
        "pre-V47 ROM) and not a minimum, so AmigaOS 3.9's own requirement went",
        "unstated and unchecked. Approximating it through `rom-older-than`",
        "would have asserted something false, which is why it was filed rather",
        "than invented.",
        "",
        "**The number is the disc's own, read off the artefact rather than",
        "recalled.** `OS-Version3.9/OS3.9Install` — the release's own Installer",
        "script, `$VER: Install 45.0 (24.11.2000)` — carries the refusal string",
        "`#bad-kick`:",
        "",
        "    You have to install Kickstart 3.1 ROMs before installing",
        "    Workbench 3.9.",
        "",
        "and, in the German catalogue beside it, `Sie müssen Kickstart 3.1 und",
        "min. Workbench 3.0 zur Nutzung von Workbench 3.9 vorinstallieren.`",
        "Kickstart 3.1 is V40, so 40 is the floor the release itself names.",
        "(Extracted from the owner's own AmigaOS39.iso with 7-Zip, 2026-08-20.",
        "The manual's `installation/book-main1.html` states the *hardware*",
        "floor — 68020, 6 MB Chip and 4 MB Fast on pre-A1200 models — and no",
        "Kickstart version at all, which is why the installer and not the",
        "manual is cited here.)",
        "",
        "**Declared on `workbench-base`, which is `required`, so it is a",
        "statement and not a switch.** A `Condition` can only ever turn a",
        "component *on* (`plan::resolve_components_on`), and this one is on",
        "regardless; what the declaration changes is what the finished tree",
        "records. `plan::rom_requirement` reads it into",
        "`PairedRom::requires_major`, and that is what G9's",
        "`core::rom::pairing::compare` puts to the card's own Kickstart before",
        "the destructive step runs — a card carrying a V37 or V39 ROM now",
        "reports `Unsuitable { needs: 40 }` instead of nothing at all.",
        "",
        "One thing this deliberately does **not** do: refuse to build the tree.",
        "A 3.9 tree is assembled on the host and the Kickstart it will meet is",
        "the card's, which may not be the one paired at plan time — and may not",
        "exist yet. The requirement is recorded and checked where it can",
        "actually be acted on."
      ],
      "condition": { "condition": "rom-at-least", "major": 40 },
      "rules": [
        { "from": "OS-VERSION3.9/WORKBENCH3.5/C",         "to": "C",         "kind": "subtree" },
        { "from": "OS-VERSION3.9/WORKBENCH3.5/CLASSES",   "to": "Classes",   "kind": "subtree" },
        { "from": "OS-VERSION3.9/WORKBENCH3.5/DEVS",      "to": "Devs",      "kind": "subtree" },
        { "from": "OS-VERSION3.9/WORKBENCH3.5/EXPANSION", "to": "Expansion", "kind": "subtree" },
        { "from": "OS-VERSION3.9/WORKBENCH3.5/FONTS",     "to": "Fonts",     "kind": "subtree" },
        { "from": "OS-VERSION3.9/WORKBENCH3.5/L",         "to": "L",         "kind": "subtree" },
        { "from": "OS-VERSION3.9/WORKBENCH3.5/LIBS",      "to": "Libs",      "kind": "subtree" },
        { "from": "OS-VERSION3.9/WORKBENCH3.5/PREFS",     "to": "Prefs",     "kind": "subtree" },
        { "from": "OS-VERSION3.9/WORKBENCH3.5/REXXC",     "to": "Rexxc",     "kind": "subtree" },
        { "from": "OS-VERSION3.9/WORKBENCH3.5/S",         "to": "S",         "kind": "subtree" },
        { "from": "OS-VERSION3.9/WORKBENCH3.5/STORAGE",   "to": "Storage",   "kind": "subtree" },
        { "from": "OS-VERSION3.9/WORKBENCH3.5/SYSTEM",    "to": "System",    "kind": "subtree" },
        { "from": "OS-VERSION3.9/WORKBENCH3.5/TOOLS",     "to": "Tools",     "kind": "subtree" },
        { "from": "OS-VERSION3.9/WORKBENCH3.5/UTILITIES", "to": "Utilities", "kind": "subtree" }
      ]
    },
    {
      "id": "locale-base",
      "label_key": "osinstall.components.name.os39.locale",
      "media": "AmigaOS3.9",
      "required": false,
      "rules": [
        { "from": "OS-VERSION3.9/LOCALE/CATALOGS",  "to": "Locale/Catalogs",  "kind": "subtree" },
        { "from": "OS-VERSION3.9/LOCALE/COUNTRIES", "to": "Locale/Countries", "kind": "subtree" },
        { "from": "OS-VERSION3.9/LOCALE/LANGUAGES", "to": "Locale/Languages", "kind": "subtree" },
        { "from": "OS-VERSION3.9/LOCALE/HELP",      "to": "Locale/Help",      "kind": "subtree" }
      ]
    },
    {
      "_why_a_second_layer_and_not_a_bigger_workbench_base": [
        "ART-169, and the first thing to say is what this component is *not*:",
        "it is not a correction of `workbench-base`'s source. Both trees are",
        "real and neither is a complete volume. Measured with `7z l -slt`",
        "against the owner's own AmigaOS39.iso (2026-08-19):",
        "",
        "  OS-Version3.9/Workbench3.5   673 rows, 16 drawers",
        "  OS-Version3.9/Workbench3.9   854 rows, 13 drawers",
        "",
        "`Workbench3.9` has no `L`, no `Expansion`, no `Rexxc` and no `T`; its",
        "own `S` holds `bg`, `fg`, `fork`, `SetFont`, `SetKeyboard`, `History`",
        "and three ARexx scripts and **no `Startup-Sequence`**; and there is no",
        "`Workbench3.9/C/Version` at all. Its `C` holds 29 commands where 3.5's",
        "holds 52, and 12 of the 29 are replacements while 17 are new. Its",
        "`Libs/workbench.library` is 199,852 bytes against 3.5's 193,400 — a",
        "different, larger file, not the same one twice.",
        "",
        "So `Workbench3.9` ships only what changed, which is exactly why a 3.9",
        "CD carries a `Workbench3.5` drawer at all: the real Installer lays 3.5",
        "and then lays 3.9 over it. `workbench-base` was never installing the",
        "wrong tree — it was stopping after the first layer, which is why the",
        "tree booted clean (ART-169's own evidence) while reporting",
        "`Workbench 44.5` and failing `C:LoadMonDrvs` on the first line of the",
        "Startup-Sequence it *did* place. The Startup-Sequence in",
        "`Workbench3.5/S` is already the 3.9 one; the command it calls lives",
        "only in this layer.",
        "",
        "The rules below are read off the disc's own `Workbench3.9` drawer",
        "listing, **not** copied from `workbench-base` — the two lists differ by",
        "five drawers in each direction, and copying one recipe's rule list into",
        "another is the mistake BoingBag 3.9-2's own JSON records nearly making."
      ],
      "_why_overrides_and_why_last": [
        "`plan()` emits items in recipe-declaration order and `apply`'s writer",
        "lets the last writer win, so an overlay has to be declared *after* the",
        "layers it covers. It is last in this array for that reason and no",
        "other.",
        "",
        "`overrides` names both, because both are really collided with:",
        "",
        "  workbench-base — 12 of this layer's 29 `C` commands replace a 3.5",
        "    command of the same name, and every other drawer overlaps the same",
        "    way. `detect_collisions` refuses an undeclared claim outright, so",
        "    this is load-bearing rather than documentary.",
        "",
        "  locale-base — measured, not assumed. `Workbench3.9/Locale` and",
        "    `OS-Version3.9/Locale` are *overlapping and complementary*, and",
        "    neither contains the other:",
        "      Countries  32 of 33 shared (locale-base carries one more)",
        "      Catalogs   this layer's drawer is EMPTY; locale-base has 645",
        "      Languages  this layer's drawer is EMPTY; locale-base has 22",
        "      Help       disjoint: this layer has only ViNCEd.guide(+.info),",
        "                 the 3.9 console handler's guide; locale-base has 45",
        "                 other guides and not that one",
        "      Flags      61 here (Countries *and* a whole Keymaps set) against",
        "                 locale-base's 20, only 16 shared",
        "      Providers  4, identical",
        "    So `locale-base` is not redundant — it is the only source of any",
        "    catalog or any `.language` file, which is what ART-162 added it",
        "    for — and this layer is not redundant either.",
        "",
        "One consequence stated rather than left to be discovered: this rule",
        "places `Locale` whole, so `Locale/Flags` (61 rows) and",
        "`Locale/Providers` (4) reach the volume, and `locale-base` places",
        "neither. **Ruled on in Task 8, fix round 2 (F3): shipping them is",
        "accepted.** Both are part of AmigaOS 3.9, and `locale-base`'s own",
        "note excluded them because they are outside *that* component's",
        "scope — locale selection — not because anything judged them",
        "unwanted. That note has been corrected to say so from its side too,",
        "so the two read together rather than contradicting each other."
      ],
      "id": "workbench-39",
      "label_key": "osinstall.components.name.os39.overlay",
      "media": "AmigaOS3.9",
      "required": true,
      "overrides": ["workbench-base", "locale-base"],
      "rules": [
        { "from": "OS-VERSION3.9/WORKBENCH3.9/C",         "to": "C",         "kind": "subtree" },
        { "from": "OS-VERSION3.9/WORKBENCH3.9/CLASSES",   "to": "Classes",   "kind": "subtree" },
        { "from": "OS-VERSION3.9/WORKBENCH3.9/DEVS",      "to": "Devs",      "kind": "subtree" },
        { "from": "OS-VERSION3.9/WORKBENCH3.9/FONTS",     "to": "Fonts",     "kind": "subtree" },
        { "from": "OS-VERSION3.9/WORKBENCH3.9/LIBS",      "to": "Libs",      "kind": "subtree" },
        { "from": "OS-VERSION3.9/WORKBENCH3.9/LOCALE",    "to": "Locale",    "kind": "subtree" },
        { "from": "OS-VERSION3.9/WORKBENCH3.9/PREFS",     "to": "Prefs",     "kind": "subtree" },
        { "from": "OS-VERSION3.9/WORKBENCH3.9/S",         "to": "S",         "kind": "subtree" },
        { "from": "OS-VERSION3.9/WORKBENCH3.9/STORAGE",   "to": "Storage",   "kind": "subtree" },
        { "from": "OS-VERSION3.9/WORKBENCH3.9/SYSTEM",    "to": "System",    "kind": "subtree" },
        { "from": "OS-VERSION3.9/WORKBENCH3.9/TOOLS",     "to": "Tools",     "kind": "subtree" },
        { "from": "OS-VERSION3.9/WORKBENCH3.9/UTILITIES", "to": "Utilities", "kind": "subtree" },
        { "from": "OS-VERSION3.9/WORKBENCH3.9/WBSTARTUP", "to": "WBStartup", "kind": "subtree" }
      ]
    },
    {
      "_why_this_component_exists": [
        "ART-159's hazard 2, the half that is the owner's own language. Measured off",
        "their AmigaOS39.iso on 2026-08-23 by walking the disc's Primary directory",
        "records directly — not through ART, and not through 7-Zip, so what follows is",
        "the disc's own bytes rather than a tool's reading of them.",
        "",
        "`OS-Version3.9/Special-Locale` is 245 nodes in three languages: Czech 123,",
        "Turkish 65, Russian 57. The Czech branch is what the drawer's name suggests —",
        "Devs, Fonts, L, Locale and its own `Locale Installation` script. **The Turkish",
        "branch is fonts and nothing else**: no keymap, no printer driver, no",
        "`L/FileSystem_Trans`, no install script. All 65 of its nodes sit under one",
        "`fonts` drawer — 13 families, 13 `.font` descriptors and 37 size files.",
        "",
        "They are the ISO-8859-9 (Latin-5) set, which is the point: a Turkish system",
        "built from this disc without them has the catalogs and not the glyphs. ş, ğ, ı",
        "and İ are exactly the characters Latin-1's own font set has no place for. That",
        "is ART-127's shape — a tree that builds and verifies clean and is wrong on a",
        "running Amiga.",
        "",
        "**The disc's own Installer never places them.** `OS-Version3.9/OS3.9Install`",
        "(`$VER: Install 45.0`, 204 064 bytes, read out of the same ISO) does not",
        "contain the string `Special-Locale` anywhere — nor `Locale.Euro`. Both drawers",
        "are supplementary material a user installs by hand, which is what makes them",
        "ordinary optional components here and `required: false` rather than a",
        "judgement call.",
        "",
        "**Non-ASCII in a `from` path, and why it is safe.** The Primary tree spells the",
        "drawer `TÜRKÇE`, raw bytes `54 DC 52 4B C7 45` — Latin-1 Ü and Ç, uppercased",
        "the way ISO9660 uppercases every Primary name. `validate_path` accepts it",
        "(`check_name` allows any Latin-1 character), and it matches the disc byte for",
        "byte, so nothing here rests on `fold_amiga_case`'s Latin-1 range. Nor does it",
        "reach ART-113: every *destination* below is ASCII, so no non-ASCII name is ever",
        "asked of a PFS3 volume."
      ],
      "_why_thirteen_named_families_and_not_the_whole_drawer": [
        "One rule saying `fonts → Fonts` would be shorter and would be a worse",
        "statement. `Fonts` is a drawer `workbench-base` owns, and taking a base drawer",
        "whole is allowed only by declaring an override over the base",
        "(`no_toolkit_disk_takes_a_whole_drawer_the_base_already_owns`). Declaring one",
        "here would say this layer replaces the system's fonts, and it does not:",
        "measured against the same disc, the base `Fonts` drawer holds COURIER, DIAMOND,",
        "EMERALD, GARNET, HELVETICA, OPAL, RUBY, SAPPHIRE, TIMES, TOPAZ, RAFALLBACK,",
        "_BULLET, _BULLET_OUTLINES (plus ACTION and TOPAZ6 from the 3.9 overlay), and",
        "**not one** of the thirteen names below. Zero collisions, so zero overrides,",
        "and the rules say which thirteen families arrive instead of implying a",
        "replacement that never happens.",
        "",
        "**Destinations keep the disc's own spelling, and the paragraph that used to",
        "stand here got that wrong in a way only a booted Amiga could show — ART-225.**",
        "It said the disc's spelling is all-caps because that is what the ISO9660",
        "Primary tree carries, and that AmigaDOS's case-insensitivity made the",
        "difference cosmetic. Both halves were false. This disc carries **Rock Ridge**",
        "name entries and `core/iso/directory.rs` prefers them (\"The Rock Ridge name",
        "wins when there is one\"), so what `CdSource` actually returns — and what every",
        "other component of this recipe therefore places — is the disc's mixed-case",
        "Amiga spelling: ART's own manifest records `Fonts/courier.font` and",
        "`Fonts/CGTimes.font`, not `COURIER.FONT`. And the difference is not cosmetic:",
        "`diskfont.library` matches the `.font` suffix **case-sensitively**, so the",
        "thirteen descriptors this component placed as `TOPAZ-ISO9.FONT` were invisible",
        "to `AvailFonts`, to every font requester, and to the system. Measured on the",
        "owner's own machine by lowering the suffix on exactly one of the thirteen and",
        "reopening the requester: that one appeared, the other twelve did not.",
        "",
        "So the `to` paths below are the disc's own leaf names, copied rather than",
        "retyped — including the disc's own inconsistencies (`FuturaB-ISO9.font` beside",
        "`courier-iso9.font`, `XHelvetica-iso9.font` beside `XCourier-ISO9.font`, and a",
        "family drawer whose case differs from its own descriptor's). Copying an",
        "inconsistency is right and tidying it would be ART renaming the user's file.",
        "",
        "`from` is deliberately **not** rewritten to match. It resolves today — the real",
        "run refuses nothing — because lookup is case-insensitive, and the drawer above",
        "these does not carry the same name in its Rock Ridge entry as in its Primary",
        "one, so retyping the path in the other spelling would be a change with no",
        "upside and a real risk."
      ],
      "id": "special-locale-turkish",
      "label_key": "osinstall.components.name.os39.turkishFonts",
      "media": "AmigaOS3.9",
      "required": false,
      "rules": [
        { "from": "OS-VERSION3.9/SPECIAL-LOCALE/TÜRKÇE/FONTS/COURIER-ISO9", "to": "Fonts/courier-iso9", "kind": "subtree" },
        { "from": "OS-VERSION3.9/SPECIAL-LOCALE/TÜRKÇE/FONTS/COURIER-ISO9.FONT", "to": "Fonts/courier-iso9.font", "kind": "file" },
        { "from": "OS-VERSION3.9/SPECIAL-LOCALE/TÜRKÇE/FONTS/DIAMOND-ISO9", "to": "Fonts/diamond-iso9", "kind": "subtree" },
        { "from": "OS-VERSION3.9/SPECIAL-LOCALE/TÜRKÇE/FONTS/DIAMOND-ISO9.FONT", "to": "Fonts/diamond-iso9.font", "kind": "file" },
        { "from": "OS-VERSION3.9/SPECIAL-LOCALE/TÜRKÇE/FONTS/EMERALD-ISO9", "to": "Fonts/emerald-iso9", "kind": "subtree" },
        { "from": "OS-VERSION3.9/SPECIAL-LOCALE/TÜRKÇE/FONTS/EMERALD-ISO9.FONT", "to": "Fonts/emerald-iso9.font", "kind": "file" },
        { "from": "OS-VERSION3.9/SPECIAL-LOCALE/TÜRKÇE/FONTS/FUTURAB-ISO9", "to": "Fonts/futurab-iso9", "kind": "subtree" },
        { "from": "OS-VERSION3.9/SPECIAL-LOCALE/TÜRKÇE/FONTS/FUTURAB-ISO9.FONT", "to": "Fonts/FuturaB-ISO9.font", "kind": "file" },
        { "from": "OS-VERSION3.9/SPECIAL-LOCALE/TÜRKÇE/FONTS/GARNET-ISO9", "to": "Fonts/garnet-iso9", "kind": "subtree" },
        { "from": "OS-VERSION3.9/SPECIAL-LOCALE/TÜRKÇE/FONTS/GARNET-ISO9.FONT", "to": "Fonts/garnet-iso9.font", "kind": "file" },
        { "from": "OS-VERSION3.9/SPECIAL-LOCALE/TÜRKÇE/FONTS/PERSONAL-ISO9", "to": "Fonts/personal-iso9", "kind": "subtree" },
        { "from": "OS-VERSION3.9/SPECIAL-LOCALE/TÜRKÇE/FONTS/PERSONAL-ISO9.FONT", "to": "Fonts/Personal-ISO9.font", "kind": "file" },
        { "from": "OS-VERSION3.9/SPECIAL-LOCALE/TÜRKÇE/FONTS/TIMES-ISO9", "to": "Fonts/times-iso9", "kind": "subtree" },
        { "from": "OS-VERSION3.9/SPECIAL-LOCALE/TÜRKÇE/FONTS/TIMES-ISO9.FONT", "to": "Fonts/Times-ISO9.font", "kind": "file" },
        { "from": "OS-VERSION3.9/SPECIAL-LOCALE/TÜRKÇE/FONTS/TOPAZ-ISO9", "to": "Fonts/topaz-iso9", "kind": "subtree" },
        { "from": "OS-VERSION3.9/SPECIAL-LOCALE/TÜRKÇE/FONTS/TOPAZ-ISO9.FONT", "to": "Fonts/Topaz-ISO9.font", "kind": "file" },
        { "from": "OS-VERSION3.9/SPECIAL-LOCALE/TÜRKÇE/FONTS/TOPAZT", "to": "Fonts/topazt", "kind": "subtree" },
        { "from": "OS-VERSION3.9/SPECIAL-LOCALE/TÜRKÇE/FONTS/TOPAZT.FONT", "to": "Fonts/topazt.font", "kind": "file" },
        { "from": "OS-VERSION3.9/SPECIAL-LOCALE/TÜRKÇE/FONTS/XCOURIER-ISO9", "to": "Fonts/xcourier-iso9", "kind": "subtree" },
        { "from": "OS-VERSION3.9/SPECIAL-LOCALE/TÜRKÇE/FONTS/XCOURIER-ISO9.FONT", "to": "Fonts/XCourier-ISO9.font", "kind": "file" },
        { "from": "OS-VERSION3.9/SPECIAL-LOCALE/TÜRKÇE/FONTS/XEN-ISO9", "to": "Fonts/xen-iso9", "kind": "subtree" },
        { "from": "OS-VERSION3.9/SPECIAL-LOCALE/TÜRKÇE/FONTS/XEN-ISO9.FONT", "to": "Fonts/Xen-ISO9.font", "kind": "file" },
        { "from": "OS-VERSION3.9/SPECIAL-LOCALE/TÜRKÇE/FONTS/XEN-WIDE-ISO9", "to": "Fonts/xen-wide-iso9", "kind": "subtree" },
        { "from": "OS-VERSION3.9/SPECIAL-LOCALE/TÜRKÇE/FONTS/XEN-WIDE-ISO9.FONT", "to": "Fonts/Xen-Wide-ISO9.font", "kind": "file" },
        { "from": "OS-VERSION3.9/SPECIAL-LOCALE/TÜRKÇE/FONTS/XHELVETICA-ISO9", "to": "Fonts/xhelvetica-iso9", "kind": "subtree" },
        { "from": "OS-VERSION3.9/SPECIAL-LOCALE/TÜRKÇE/FONTS/XHELVETICA-ISO9.FONT", "to": "Fonts/XHelvetica-iso9.font", "kind": "file" }
      ]
    },
    {
      "_why_this_component_exists": [
        "ART-159's hazard 2, the other half. `OS-Version3.9/Locale.Euro/Countries` is",
        "nine `.country` files — Österreich, Belgie, Belgique, Deutschland, España,",
        "France, Italia, Nederland, Portugal — the euro-currency replacements for",
        "country files the base release already writes.",
        "",
        "**Measured, because same-size files are the easy thing to get wrong.** All",
        "nine are byte-for-byte different from their namesakes in",
        "`OS-Version3.9/Locale` (what `locale-base` places) and in",
        "`OS-Version3.9/Workbench3.9/Locale` (what `workbench-39` places), while being",
        "*exactly* the same length as them — 586, 588, 590, 592 bytes. A `.country`",
        "file is a fixed-shape structure, so equal size proves nothing and only the",
        "SHA-256s settle it. Compared on 2026-08-23 by reading the disc's own directory",
        "records: 9 of 9 DIFFERENT.",
        "",
        "**The disc carries two euro sets and this is the newer one.**",
        "`Workbench3.5/Storage/Locale/Countries-Euro` holds ten files (nine plus a",
        "second spelling, `Belgie` and `België`, identical to each other) and every one",
        "of them differs from the nine here too. That set reaches the tree already, as",
        "`Storage/LOCALE/COUNTRIES-EURO`, where nothing selects it — Storage is the",
        "shelf, not the system. This component is the one that puts euro country files",
        "where `locale.library` actually reads them.",
        "",
        "`overrides` names both layers because it really replaces both, and it is",
        "declared **last** for the reason `workbench-39`'s own note gives: `plan()`",
        "emits items in recipe-declaration order and `apply`'s writer lets the last",
        "writer win, so an overlay declared above the layers it covers is silently",
        "reverted. `workbench-base` is not named — its `Storage` rule lands somewhere",
        "else entirely, and an override over a component that is not in the way would",
        "be a licence to overwrite something nobody measured.",
        "",
        "Like `special-locale-turkish` above, the disc's own Installer places none of",
        "this: `OS3.9Install` does not mention `Locale.Euro` at all."
      ],
      "id": "locale-euro",
      "label_key": "osinstall.components.name.os39.euroCountries",
      "media": "AmigaOS3.9",
      "required": false,
      "overrides": ["locale-base", "workbench-39"],
      "rules": [
        { "from": "OS-VERSION3.9/LOCALE.EURO/COUNTRIES", "to": "Locale/Countries", "kind": "subtree" }
      ]
    }
  ]
}
