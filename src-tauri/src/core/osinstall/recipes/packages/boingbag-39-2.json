{
  "_why_this_differs_from_boingbag_39_1": [
    "Measured separately (Task 4) rather than copied from BoingBag 3.9-1's own",
    "top level, per the task brief's own warning that the two were never the",
    "same shape. BoingBag 3.9-2's payload ZIP top level is twelve directories,",
    "not thirteen: it carries `Rexxc/`, which 3.9-1 does not, and it carries",
    "neither `Fonts/` nor `Storage/`, which 3.9-1 does. Confirmed with",
    "`7z l` against the owner's own BoingBag39-2.lha, member `AmigaOS-Update`:",
    "121 files, 26 folders (147 entries). The task brief's own '113 entries'",
    "was the *wrapper* LHA's own entry count, not the payload ZIP's — corrected",
    "in the Task 4 review round; see `task-4-report.md`'s Fix round 1 section."
  ],
  "id": "boingbag-39-2",
  "name": "BoingBag 3.9-2",
  "releases": [
    "AmigaOS 3.9"
  ],
  "media": "BoingBag3.9-2",
  "_why_distinguished_by_repeats_the_member": [
    "ART-167. Two of the owner's archives carry the top-level directory",
    "`BoingBag3.9-2`: this one and `BoingBag39-2-Contribution.lha`, which is",
    "the same BoingBag's contributed extras. Measured (44 archives walked,",
    "scripts/lha-package-identity.py, 2026-08-20): this archive's second",
    "level holds `AmigaOS-Update`, `C`, `Install`, `Installer`, `Manuals`",
    "and `XAD-Update`; the Contribution archive's holds `Contribution` and",
    "the readmes, and no payload member at all.",
    "",
    "So the string below is the same one `member` names, and it is written",
    "twice on purpose. `member` says where this package's payload lives;",
    "`distinguished_by` says which archive is this package's. They are not",
    "the same fact, and the same measurement shows it: `AmigaOS-Update` is",
    "carried by `BoingBag39-1.lha` too, so as an identity on its own it",
    "separates nothing — it only settles this pair because the other",
    "claimant of `BoingBag3.9-2` happens to lack it. Folding the two fields",
    "together would encode a coincidence as a rule."
  ],
  "distinguished_by": "AmigaOS-Update",
  "member": "AmigaOS-Update",
  "_why_host_placement_block": [
    "ART-166, measured in Task 8's real run and confirmed three independent ways (ART's own reader, 7-Zip 26.02, the raw general-purpose flag word 0x0003 with bit 0 set): the `AmigaOS-Update` member named above is a ZIP whose every entry is ZipCrypto-encrypted — 233 of 233 entries in 3.9-1's payload, 147 of 147 in 3.9-2's. The password belongs to the BoingBag's own `Updater`, which the wrapper LHA carries beside the payload and which has to run *on* an Amiga; every established distribution builder (HstWB Installer, AmiKit, AmigaSYS, ClassicWB) installs a BoingBag by running that Updater inside an emulator rather than by decrypting anything, and the owner's recorded decision is that ART will write no bypass.  So the rules below are right and unreachable, and spec §10/§89 says ART must not offer what it cannot do. Declaring the block here — data, not code — is what makes the screen refuse the tick with a translated sentence naming the Updater, instead of accepting it and meeting the reader's own raw English `Password required to decrypt file` after the user already confirmed. **That prediction was wrong, and is corrected here rather than left standing (2026-08-25).** It read: \"The day the Amiga-side install round lands, this one key is deleted and nothing else changes.\" That round landed on 2026-08-21 and both BoingBags installed on the owner's own material through ART's own compose-then-install path - and this key STAYS. The block is about placing the payload's files from the host, which is as impossible as it ever was: the Amiga's own Updater writes them, inside the emulator, after the package's own code decrypts its own payload. Deleting this key would put a live tick back on a screen that still cannot do what the tick promises."
  ],
  "host_placement_block": "encrypted-payload",
  "_why_amiga_installer": [
    "Measured separately from 3.9-1's rather than copied, the same rule this",
    "file's own top comment follows (2026-08-20, 7-Zip 26.02 against the",
    "owner's BoingBag39-2.lha). It happens to agree, and the agreement is a",
    "reading, not an assumption:",
    "",
    "  1. `BoingBag3.9-2\\C\\Updater` is a real entry, 42 676 bytes — a",
    "     different build from 3.9-1's 25 588-byte one — so the path relative",
    "     to the top-level directory is `C/Updater` here too. This archive",
    "     also carries `BoingBag3.9-2\\C\\Installer`, which 3.9-1 does not;",
    "     that is the Installer *engine* the `Install` script needs, not the",
    "     thing that installs this package.",
    "",
    "  2. This package's `Install` script, line 1615, runs",
    "     `C/Updater AmigaOS-Update \"<target>\"` — identical in shape to",
    "     3.9-1's line 858.",
    "",
    "One thing 3.9-1 has no counterpart for: line 1632 runs the Updater a",
    "second time, `C/Updater XAD-Update \"<target>\"`, guarded by",
    "`(if (= #install-xad-update 1) ...)` — a choice the script's own",
    "requester puts to the user. It is NOT declared here. `AmigaInstaller`",
    "describes one run, this round installs what the package is (the AmigaOS",
    "update), and declaring an optional extra as though it were part of the",
    "package would install something the user was never asked about. The day",
    "ART offers that choice, it is a second declaration, not a code path."
  ],
  "_why_required_medium": [
    "ART-193, and read out of *this* package's own Updater rather than",
    "copied from 3.9-1's, the same rule the block above follows. Its",
    "printable strings (2026-08-21, the owner's own BoingBag39-2.lha,",
    "`$VER: Updater 45.19 (9.11.2001)`):",
    "",
    "  Checking AmigaOS 3.9 CD-ROM ...",
    "  Failed to check AmigaOS 3.9 CD-ROM.",
    "  Did you really insert a original AmigaOS 3.9 CD-ROM",
    "  into a mounted CD-ROM drive?",
    "  AmigaOS3.9:",
    "  AmigaOS3.9:Videos/Angels.avi",
    "  AmigaOS3.9:Audio/Circle - Orbital.mp3",
    "  AmigaOS3.9:Audio/Beatmaster K - Rumba Lat.mp3",
    "  AmigaOS XL:Multimedia/Angels.avi",
    "",
    "So this build checks four named files rather than three, spells",
    "`Circle - Orbital.mp3` the way the disc actually does (45.15 does not),",
    "and will also accept an `AmigaOS XL:` volume. Only `AmigaOS3.9` is",
    "declared here: ART supplies one medium, the owner has that disc, and a",
    "second alternative nobody has cannot be checked against anything.",
    "Declaring what has not been tried is the §89 mistake from the other",
    "side."
  ],
  "amiga_installer": {
    "_why_post_install": [
      "ART-227, and this one is the whole reason the mechanism exists.",
      "",
      "BoingBag 2's `Updater` writes its ROM update as",
      "`Devs/AmigaOS ROM Update.BB39-2` — **321 768 bytes** — and leaves it there,",
      "beside the CD's own 127 956-byte `Devs/AmigaOS ROM Update`, under a name",
      "`SetPatch` does not load. Measured on 2026-08-24 on the tree ART's own run",
      "produced: both files present, the old one still the active one. So a tree that",
      "reports itself BoingBag'd, and answers `Workbench 45.3` when asked, is running",
      "the ROM update it had before. Nothing fails; the newer file is simply never",
      "read.",
      "",
      "HstWB Installer does the same move — back the old one aside, copy the new one",
      "over it — which is where this was found. ART does it on the host instead of in",
      "the boot script, for the reasons `core::amigainstall::finish` gives.",
      "",
      "**`NSDPatch.cfg` is deliberately left alone.** BoingBag 1 leaves",
      "`Devs/NSDPatch.cfg-BB3.9-1` and BoingBag 2 leaves `Devs/NSDPatch.cfg.BB39-2`",
      "(the two packages do not even agree on the punctuation), and neither is rotated",
      "here. That file is user-editable configuration; a reference copy beside it is",
      "the right shape, and HstWB reaches the same conclusion by touching neither."
    ],
    "post_install": [
      {
        "step": "replace-keeping-backup",
        "target": "Devs/AmigaOS ROM Update",
        "replacement": "Devs/AmigaOS ROM Update.BB39-2"
      }
    ],
    "_why_follow_ups": [
      "ART-280, and it was measured before it was declared. On a chain ART",
      "itself produced (clean AmigaOS 3.9 tree -> BoingBag 1 -> BoingBag 2,",
      "every file of every state hashed, 2026-09-08) `Libs/xadmaster.library`",
      "reads `xadmaster 9.0 (25.11.2000)` clean, `9.1 (05.01.2001)` after",
      "BoingBag 1 - whose payload carries that 105 368-byte build - and",
      "**9.1 still** after BoingBag 2. So this package's second payload was",
      "never applied, and 9.1 is below the 10 the gate names.",
      "",
      "What it applies: `BoingBag3.9-2/XAD-Update` is a second ZipCrypto",
      "archive inside the same wrapper, 152 837 bytes, 39 entries, every one",
      "encrypted - `Libs/xadmaster.library` at 110 100 bytes, 26 `Libs/xad/*`",
      "clients and 10 `C/` tools. Nothing here decrypts it: the package's own",
      "`Updater` does, on the Amiga, exactly as it does for `AmigaOS-Update`",
      "(ART-166, and the owner's recorded ruling that ART writes no bypass).",
      "",
      "The shape is HstWB Installer's, read from its own source rather than",
      "reasoned about - the one distribution builder whose scripts can be",
      "read (MIT), `amiga/amiga-os-3.9/S/Amiga-OS-3.9/Install-Boing-Bag-2`",
      "lines 32-36:",
      "",
      "  ; run xad updater, if xadmaster.library version is less than 10",
      "  Version >>SYS:hstwb-installer.log \"SYS:Libs/xadmaster.library\" 10 FILE",
      "  IF WARN",
      "    SYS:T/BoingBags/BoingBag3.9-2/C/Updater SYS:T/BoingBags/BoingBag3.9-2/XAD-Update \"SYS:\"",
      "  ENDIF",
      "",
      "Two things this is NOT, both of which an earlier round got wrong and",
      "which are corrected here rather than left standing (2026-09-08):",
      "",
      "  1. NOT a second emulator run. HstWB does it in the same boot, four",
      "     lines after the first invocation, and so does ART - it is more",
      "     lines in the one script `workvol::startup_sequence` already",
      "     writes, not another ~140 s cycle.",
      "  2. NOT behind a requester. The `#install-xad-update` requester lives",
      "     in this package's own `Install` script, and ART does not run that",
      "     script: it runs `C/Updater` directly. Nobody is asked anything by",
      "     the program ART actually launches.",
      "",
      "`program` and `args` are relative to this package's drawer, exactly",
      "like `amiga_installer.program` above; the target volume is appended by",
      "the composer because it is a fact about the run. The gate's `path` is",
      "relative to the **system** volume, and the script prefixes it.",
      "",
      "One trap, recorded because it is the reason this is a designed change",
      "and not a one-line append: `Version ... FILE` signals `older than",
      "that` by SETTING WARN - it is the answer, not an error - so a gate",
      "emitted above the script's own `If Warn` would overwrite the",
      "installer's verdict with the gate's, and every successful run on a",
      "tree with an old `xadmaster` would be reported as `the installer said",
      "no`. The follow-up block is emitted strictly below the result capture."
    ],
    "follow_ups": [
      {
        "program": "C/Updater",
        "args": [
          "XAD-Update"
        ],
        "unless_file_version_at_least": {
          "path": "Libs/xadmaster.library",
          "version": 10
        }
      }
    ],
    "program": "C/Updater",
    "args": [
      "AmigaOS-Update"
    ],
    "required_medium": {
      "volume": "AmigaOS3.9",
      "name": "the original AmigaOS 3.9 CD-ROM"
    }
  },
  "_why_chain_position": "3 of the material's own order. BoingBag 3.9-2's own Install script reads version.library off the target and requires revision >= 2, which is BoingBag 3.9-1 applied - the arrow between rows 2 and 3, enforced by the package rather than advised by prose.",
  "chain_position": 3,
  "requires": [
    "boingbag-39-1"
  ],
  "overrides": [
    "workbench-base",
    "boingbag-39-1"
  ],
  "rules": [
    {
      "from": "C",
      "to": "C",
      "kind": "subtree"
    },
    {
      "from": "Classes",
      "to": "Classes",
      "kind": "subtree"
    },
    {
      "from": "Devs",
      "to": "Devs",
      "kind": "subtree"
    },
    {
      "from": "L",
      "to": "L",
      "kind": "subtree"
    },
    {
      "from": "Libs",
      "to": "Libs",
      "kind": "subtree"
    },
    {
      "from": "Prefs",
      "to": "Prefs",
      "kind": "subtree"
    },
    {
      "from": "Rexxc",
      "to": "Rexxc",
      "kind": "subtree"
    },
    {
      "from": "S",
      "to": "S",
      "kind": "subtree"
    },
    {
      "from": "System",
      "to": "System",
      "kind": "subtree"
    },
    {
      "from": "Tools",
      "to": "Tools",
      "kind": "subtree"
    },
    {
      "from": "Utilities",
      "to": "Utilities",
      "kind": "subtree"
    },
    {
      "from": "WBStartup",
      "to": "WBStartup",
      "kind": "subtree"
    }
  ]
}
