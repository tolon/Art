{
  "_why_this_differs_from_boingbag_39_1": [
    "Measured separately (Task 4) rather than copied from BoingBag 3.9-1's own",
    "top level, per the task brief's own warning that the two were never the",
    "same shape. BoingBag 3.9-2's payload ZIP top level is twelve directories,",
    "not thirteen: it carries `Rexxc/`, which 3.9-1 does not, and it carries",
    "neither `Fonts/` nor `Storage/`, which 3.9-1 does. Confirmed with",
    "`7z l` against the owner's own BoingBag39-2.lha, member `AmigaOS-Update`:",
    "121 files, 26 folders (147 entries). The task brief's own '113 entries'",
    "was the *wrapper* LHA's own entry count, not the payload ZIP's — corrected",
    "in the Task 4 review round; see `task-4-report.md`'s Fix round 1 section."
  ],
  "id": "boingbag-39-2",
  "name": "BoingBag 3.9-2",
  "releases": [
    "AmigaOS 3.9"
  ],
  "media": "BoingBag3.9-2",
  "_why_distinguished_by_repeats_the_member": [
    "ART-167. Two of the owner's archives carry the top-level directory",
    "`BoingBag3.9-2`: this one and `BoingBag39-2-Contribution.lha`, which is",
    "the same BoingBag's contributed extras. Measured (44 archives walked,",
    "scripts/lha-package-identity.py, 2026-08-20): this archive's second",
    "level holds `AmigaOS-Update`, `C`, `Install`, `Installer`, `Manuals`",
    "and `XAD-Update`; the Contribution archive's holds `Contribution` and",
    "the readmes, and no payload member at all.",
    "",
    "So the string below is the same one `member` names, and it is written",
    "twice on purpose. `member` says where this package's payload lives;",
    "`distinguished_by` says which archive is this package's. They are not",
    "the same fact, and the same measurement shows it: `AmigaOS-Update` is",
    "carried by `BoingBag39-1.lha` too, so as an identity on its own it",
    "separates nothing — it only settles this pair because the other",
    "claimant of `BoingBag3.9-2` happens to lack it. Folding the two fields",
    "together would encode a coincidence as a rule."
  ],
  "distinguished_by": "AmigaOS-Update",
  "member": "AmigaOS-Update",
  "_why_payload_password": [
    "ART-166. The whole history is in `boingbag-39-1.json`'s `_why_payload_password`, which",
    "quotes the owner's 2026-09-08 reversal and the two MIT projects it follows; it is not",
    "restated here.",
    "",
    "**This key is not 3.9-1's, and it was read separately** - Emu68 Hatcher's",
    "`boingbag.py` gives BoingBag 2 `3FB6986B-B0AD6339-4FF3254B`, three groups where 3.9-1 has",
    "one. Verified against the owner's own `BoingBag39-2.lha` on 2026-09-08 with Python's",
    "`zipfile`: `AmigaOS-Update` 147 of 147 entries read clean and `XAD-Update` 39 of 39 with",
    "**the same key** - one password for both members of this wrapper, which is a fact about",
    "the archive and is why `extra_members` below carries no key of its own."
  ],
  "payload_password": "3FB6986B-B0AD6339-4FF3254B",
  "_why_extra_members": [
    "ART-280, from the host: a second payload inside the same wrapper, behind the same version",
    "gate, placed by ART instead of by the package's own program.",
    "",
    "This used to have an Amiga-side twin, `amiga_installer.follow_ups` - a second `C/Updater`",
    "invocation in the same generated boot script. Both were declared while both routes existed.",
    "The emulator route for this package was removed on 2026-09-08 (see `_why_payload_password`",
    "in `boingbag-39-1.json`), and this unit is what replaced it; the measurement that bought it",
    "- `Libs/xadmaster.library` still 9.1 after a real BoingBag 2 run, against the 10 the gate",
    "names - is the same one either way, and is recorded below.",
    "",
    "**What it carries, measured** (the owner's own `BoingBag39-2.lha`, member `XAD-Update`,",
    "every entry decrypted with the key above and listed, 2026-09-08): 39 entries, 152 837",
    "bytes, top level exactly `C` and `Libs` - 9 `C/` tools, 26 `Libs/xad/*` clients and",
    "`Libs/xadmaster.library` at 110 100 bytes. So two subtree rules cover it whole, and no",
    "rule here names a path the archive does not have.",
    "",
    "**The gate is HstWB Installer's, read from its own source** (MIT,",
    "`amiga/amiga-os-3.9/S/Amiga-OS-3.9/Install-Boing-Bag-2`, lines 32-36): `Version",
    "\"SYS:Libs/xadmaster.library\" 10 FILE` / `IF WARN` / run the updater on `XAD-Update`. The",
    "number is 10 because that is what those lines say, not because anything here chose it.",
    "",
    "**Why the gate can be read before the main member is placed.** `AmigaOS-Update` carries",
    "121 files and **no `Libs/xadmaster.library`** among them - which is also why the round-3",
    "measurement found the library still at 9.1 after a real BoingBag 2 run - so the tree's",
    "answer is the same before and after. Reading it first is what lets every file this unit",
    "would overwrite be checked against `distribution.json` before a byte is written.",
    "",
    "**How the version is read on the host, and the reading that had to be corrected.** This",
    "file carries **no `$VER:` marker at all** - searched byte by byte on 2026-09-08 across the",
    "owner's 9.0 (105 332 bytes, off the CD), 9.1 (105 368, BoingBag 1) and 10.0 (110 100,",
    "`XAD-Update`) copies, zero hits in each. What each carries at offset ~384 is its `struct",
    "Resident`: `xadmaster.library\\0` followed by `xadmaster 9.0 (25.11.2000)`. So",
    "`core::amigaver::read_id_string` reads that, anchored on the file's own name, and a",
    "`$VER:`-only reader would have answered *nothing* for the one file this gate is about."
  ],
  "extra_members": [
    {
      "member": "XAD-Update",
      "unless_file_version_at_least": {
        "path": "Libs/xadmaster.library",
        "version": 10
      },
      "rules": [
        {
          "from": "C",
          "to": "C",
          "kind": "subtree"
        },
        {
          "from": "Libs",
          "to": "Libs",
          "kind": "subtree"
        }
      ]
    }
  ],
  "_why_post_place": [
    "What the tree still needs once ART has placed this package's files. There used to be an",
    "Amiga-side twin of this list, `amiga_installer.post_install`, and it named only the first of",
    "the two steps below - so the same package installed two ways gave two different boots, and",
    "the two notes disagreed in writing about why (review F4, 2026-09-08). **That divergence is",
    "gone**: the Amiga-side route for this package was removed in the same round, this is the",
    "only list, and there is one answer to what a BoingBag 2 tree boots with.",
    "",
    "1. `Devs/AmigaOS ROM Update` - ART-227: the payload writes its 321 768-byte ROM update as",
    "   `Devs/AmigaOS ROM Update.BB39-2`, beside the CD's own 127 956-byte file, under a name",
    "   `SetPatch` does not load. Whoever puts the payload down - the Updater or ART - leaves",
    "   the same two files, so the same rotation is owed.",
    "",
    "2. `Devs/NSDPatch.cfg` - **and the reason recorded here is the artefact, not the",
    "   precedent** (review F4, 2026-09-08). An earlier note argued from Emu68 Hatcher doing it",
    "   (`(\"Devs/NSDPatch.cfg.BB39-2\", \"Devs\", \"NSDPatch.cfg\")` in its `boingbag.py`) and from",
    "   the owner's decision to follow that project. Both are true and neither is the reason.",
    "",
    "   **Asked of the tree's own files.** `C/SetPatch` in an AmigaOS 3.9 tree states",
    "   `$VER: setpatch 44.38 (8.3.2002)` and carries these two literals among its printable",
    "   strings, side by side in the same string table:",
    "",
    "       'DEVS:NSDPatch.cfg'",
    "       'DEVS:AmigaOS ROM Update'",
    "",
    "   and the tree's `S/Startup-Sequence` runs exactly one line that reaches them,",
    "   `C:SetPatch QUIET`. So `Devs/NSDPatch.cfg` is live configuration read by name at every",
    "   boot, and `Devs/NSDPatch.cfg.BB39-2` is read by nothing - the identical shape as",
    "   `Devs/AmigaOS ROM Update.BB39-2`, which step 1 above already rotates for exactly that",
    "   reason. Without the promotion the tree reports itself fully BoingBag'd while `SetPatch`",
    "   reads a year-old device table: the CD's config states `$VER: NSDPatch.cfg 43.16",
    "   (6.3.2000)` (29 929 B), BoingBag 2's states `43.21 (27.2.2001)` (32 635 B). That is",
    "   ART-227's own failure-that-does-not-crash, on a second file.",
    "",
    "   The user's own file is not lost: `replace-keeping-backup` moves it to",
    "   `Devs/NSDPatch.cfg.old` first, and refuses rather than overwriting once four generations",
    "   exist. What the promotion does cost is the recorded Amiga datestamp - the promoted file",
    "   carries no `.uaem`, so `----rwed 2000-03-06 11:38:06.00` travels to",
    "   `NSDPatch.cfg.old.uaem` with the file it describes. The protection bits are `----rwed`",
    "   either way, so nothing about the file's behaviour changes, and the Updater's own tree",
    "   shows the same shape for the ROM Update.",
    "",
    "   Recorded as a **known divergence from the Updater's own tree**: the real Updater leaves",
    "   `NSDPatch.cfg` alone, so a host placement checked against the run-8 snapshot differs on",
    "   exactly these two paths (`Devs/NSDPatch.cfg` changed, `Devs/NSDPatch.cfg.old` added),",
    "   for this reason and no other.",
    "",
    "**`rename-into-place` was asked for and `replace-keeping-backup` is what shipped.** The",
    "brief's own words were \"keeping a backup the same way\", which is this step exactly; a",
    "second variant differing only in whether the source file is left behind would be one more",
    "thing for a reader to tell apart, and leaving `Devs/NSDPatch.cfg.BB39-2` in place is also",
    "what the Updater's own tree does."
  ],
  "post_place": [
    {
      "step": "replace-keeping-backup",
      "target": "Devs/AmigaOS ROM Update",
      "replacement": "Devs/AmigaOS ROM Update.BB39-2"
    },
    {
      "step": "replace-keeping-backup",
      "target": "Devs/NSDPatch.cfg",
      "replacement": "Devs/NSDPatch.cfg.BB39-2"
    }
  ],
  "_why_chain_position": "3 of the material's own order. BoingBag 3.9-2's own Install script reads version.library off the target and requires revision >= 2, which is BoingBag 3.9-1 applied - the arrow between rows 2 and 3, enforced by the package rather than advised by prose.",
  "chain_position": 3,
  "requires": [
    "boingbag-39-1"
  ],
  "_why_overrides": [
    "`workbench-39` added 2026-09-08 for the reason `boingbag-39-1.json`'s own `_why_overrides`",
    "records at length: `overrides` is a claim about which **component the tree's manifest says",
    "placed a file**, and AmigaOS 3.9's recipe splits the Workbench between `workbench-base` and",
    "`workbench-39`. Counted against the owner's own tree: of this payload's 121 files, 84 are",
    "recorded to `workbench-39`, 17 to `workbench-base` and 20 are new. The `XAD-Update` unit",
    "below lands on 29 more `workbench-39` files and 7 new ones, and is covered by the same",
    "list - its items are this package's, under this package's component id.",
    "",
    "`boingbag-39-1` is here because BoingBag 1 rewrites many of the same paths first, and once",
    "it has, the manifest records **it** as the owner - so without this line the second BoingBag",
    "would be refused on exactly the trees the chain is designed to produce."
  ],
  "overrides": [
    "workbench-base",
    "workbench-39",
    "boingbag-39-1"
  ],
  "rules": [
    {
      "from": "C",
      "to": "C",
      "kind": "subtree"
    },
    {
      "from": "Classes",
      "to": "Classes",
      "kind": "subtree"
    },
    {
      "from": "Devs",
      "to": "Devs",
      "kind": "subtree"
    },
    {
      "from": "L",
      "to": "L",
      "kind": "subtree"
    },
    {
      "from": "Libs",
      "to": "Libs",
      "kind": "subtree"
    },
    {
      "from": "Prefs",
      "to": "Prefs",
      "kind": "subtree"
    },
    {
      "from": "Rexxc",
      "to": "Rexxc",
      "kind": "subtree"
    },
    {
      "from": "S",
      "to": "S",
      "kind": "subtree"
    },
    {
      "from": "System",
      "to": "System",
      "kind": "subtree"
    },
    {
      "from": "Tools",
      "to": "Tools",
      "kind": "subtree"
    },
    {
      "from": "Utilities",
      "to": "Utilities",
      "kind": "subtree"
    },
    {
      "from": "WBStartup",
      "to": "WBStartup",
      "kind": "subtree"
    }
  ]
}
